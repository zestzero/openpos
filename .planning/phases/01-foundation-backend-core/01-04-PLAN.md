---
phase: 01-foundation-backend-core
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified: [db/queries/inventory.sql, db/sqlc/inventory.sql.go, internal/inventory/service.go, internal/inventory/handler.go]
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04]

must_haves:
  truths:
    - "Every stock movement creates a new ledger entry"
    - "Current stock for a variant is calculated from ledger sums"
    - "Owner can record a manual adjustment (restock, waste, etc.)"
  artifacts:
    - path: "internal/inventory/service.go"
      provides: "Inventory ledger logic"
    - path: "db/queries/inventory.sql"
      provides: "Ledger aggregation queries"
  key_links:
    - from: "internal/inventory/service.go"
      to: "db/sqlc/inventory.sql.go"
      via: "sqlc query call"
---

<objective>
Implement the transactional inventory ledger system to track stock movements and provide accurate stock level reporting.

Purpose: Prevent stock discrepancies and provide a full audit trail of all inventory changes.
Output: Inventory ledger API and stock level derivation logic.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/01-foundation-backend-core/01-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Inventory Ledger queries</name>
  <files>db/queries/inventory.sql, db/sqlc/models.go</files>
  <behavior>
    - Can insert a ledger entry with type (SALE, RESTOCK, ADJUSTMENT), variant_id, delta, and reference.
    - Can calculate the sum of deltas for a specific variant_id.
    - Can list ledger entries for a variant with pagination.
  </behavior>
  <action>
    Define SQL queries in `db/queries/inventory.sql`.
    `GetStockLevel`: SUM(delta) GROUP BY variant_id.
    `ListLedgerEntries`: Ordered by created_at DESC.
    Run `sqlc generate`.
  </action>
  <verify>
    <automated>sqlc generate && ls db/sqlc/inventory.sql.go</automated>
  </verify>
  <done>Database layer for inventory ledger implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Inventory Service logic</name>
  <files>internal/inventory/service.go</files>
  <behavior>
    - AdjustStock: Validates reason code and records ledger entry.
    - GetStockLevel: Returns the current derived stock count.
    - Ensure stock cannot go below zero if business rules require (for v1, allow negative stock but log warning).
  </behavior>
  <action>
    Implement `InventoryService`.
    Include standard reason codes for adjustments.
    Implement the `DeductStock` internal method (to be used by Sales service later).
  </action>
  <verify>
    <automated>go test ./internal/inventory/...</automated>
  </verify>
  <done>Inventory service logic ready.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Inventory Handlers</name>
  <files>internal/inventory/handler.go</files>
  <action>
    Implement HTTP handlers for:
    - GET /api/inventory/variants/{id}/stock (Current level)
    - POST /api/inventory/adjust (Manual adjustment)
    - GET /api/inventory/variants/{id}/ledger (Audit trail)
  </action>
  <verify>
    <automated>go build ./cmd/server</automated>
  </verify>
  <done>Inventory API endpoints available.</done>
</task>

</tasks>

<verification>
Verify via API:
1. Check stock for a new variant (should be 0).
2. Perform "RESTOCK" adjustment of +10.
3. Check stock again (should be 10).
4. Perform "ADJUSTMENT" (waste) of -2.
5. Check stock again (should be 8).
6. View ledger for variant and verify all three entries exist with correct deltas.
</verification>

<success_criteria>
- Inventory ledger is the single source of truth for stock levels.
- Stock levels are correctly derived by summing ledger deltas.
- Every manual adjustment is traceable with a reason code and timestamp.
- API endpoints return accurate data for both summary and audit trail.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-backend-core/01-04-SUMMARY.md`
</output>
