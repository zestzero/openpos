---
phase: 01-foundation-backend-core
plan: 04
type: execute
wave: 2
depends_on: [01-03]
files_modified: [inventory/encore.service.ts, inventory/migrations/1_create_inventory.up.sql, inventory/entities.ts, inventory/api.ts]
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, PLAT-02, PLAT-03]
must_haves:
  truths:
    - "System records every stock movement in an inventory ledger"
    - "Stock balance is accurately calculated from ledger and snapshots"
    - "Idempotency prevents double-counting stock movements from offline clients"
  artifacts:
    - path: "inventory/migrations/1_create_inventory.up.sql"
      provides: "Inventory ledger and snapshot schema"
    - path: "inventory/entities.ts"
      provides: "TypeORM entities for Inventory"
    - path: "inventory/api.ts"
      provides: "Inventory and stock APIs"
  key_links:
    - from: "inventory/api.ts"
      to: "inventory/entities.ts"
      via: "TypeORM repository"
    - from: "inventory/api.ts"
      to: "catalog/entities.ts"
      via: "Verification of variant existence"
---

<objective>
Setup the core Inventory service for ledger-based stock management.
Purpose: Create the central inventory system for the POS and ERP.
Output: Operational Inventory service with Ledger, Snapshot, and Stock management endpoints.
</objective>

<execution_context>
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-backend-core/01-CONTEXT.md
@.planning/phases/01-foundation-backend-core/01-RESEARCH.md
@.planning/phases/01-foundation-backend-core/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory service scaffold and ledger schema</name>
  <files>inventory/encore.service.ts, inventory/migrations/1_create_inventory.up.sql, inventory/entities.ts</files>
  <read_first>
    - inventory/encore.service.ts (if exists)
    - inventory/entities.ts (if exists)
  </read_first>
  <action>
    1. Create `inventory/encore.service.ts` defining the inventory service and database.
    2. Create `inventory/migrations/1_create_inventory.up.sql` with tables: `inventory_ledger` (id, variant_id, delta, type: 'sale' | 'restock' | 'adjustment', reference_id: UUID, reason, client_generated_id: UUID unique), `inventory_snapshots` (id, variant_id, balance, snapshot_at).
    3. Create `inventory/entities.ts` with TypeORM entities for `InventoryLedger` and `InventorySnapshot` mapping to the migrations.
  </action>
  <verify>
    <automated>encore db shell inventory --query "SELECT * FROM inventory_ledger LIMIT 1;"</automated>
  </verify>
  <acceptance_criteria>
    - `inventory/migrations/1_create_inventory.up.sql` exists and contains correct `CREATE TABLE` statements
    - `inventory/entities.ts` defines all three entities with correct TypeORM decorators
    - `encore db shell inventory` command succeeds
  </acceptance_criteria>
  <done>Inventory service is scaffolded and ledger-based data schema is established in the database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Inventory Stock APIs</name>
  <files>inventory/api.ts</files>
  <read_first>
    - inventory/entities.ts
  </read_first>
  <behavior>
    - Ledger: Create ledger entry for stock movement (sale, restock, adjustment).
    - Idempotency: `client_generated_id` uniqueness ensures stock changes are not duplicated.
    - Stock: Calculate current stock balance for a variant as `Snapshot + Sum(Deltas since Snapshot)`.
    - Adjustment: Owner-only endpoint to manually adjust stock with a reason code.
  </behavior>
  <action>
    Implement Encore API endpoints in `inventory/api.ts`:
    1. `POST /inventory/ledger`: Creates a new ledger entry for stock movements.
    2. `POST /inventory/adjustment`: Specialized endpoint for manual stock adjustments (Owner-only).
    3. `GET /inventory/variants/:id/stock`: Calculates and returns current stock balance for a variant.
    4. Implement snapshot logic (internal function/job as per research).
  </action>
  <verify>
    <automated>encore test ./inventory/...</automated>
  </verify>
  <acceptance_criteria>
    - API endpoints correctly handle stock movements and calculations
    - Idempotency is enforced for `client_generated_id`
    - Tests verify accurate stock calculation (Snapshot + Deltas)
  </acceptance_criteria>
  <done>Inventory management APIs are functional and tested.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "System records every stock movement in an inventory ledger"
    - "Stock balance is accurately calculated from ledger and snapshots"
    - "Idempotency prevents double-counting stock movements from offline clients"
  artifacts:
    - path: "inventory/migrations/1_create_inventory.up.sql"
      provides: "Inventory ledger and snapshot schema"
    - path: "inventory/entities.ts"
      provides: "TypeORM entities for Inventory"
    - path: "inventory/api.ts"
      provides: "Inventory and stock APIs"
  key_links:
    - from: "inventory/api.ts"
      to: "inventory/entities.ts"
      via: "TypeORM repository"
    - from: "inventory/api.ts"
      to: "catalog/entities.ts"
      via: "Verification of variant existence"
</must_haves>

<success_criteria>
Inventory ledger entries are correctly created, and stock calculation logic provides accurate results for variants.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-backend-core/01-04-SUMMARY.md`
</output>
