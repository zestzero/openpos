---
phase: 01-foundation-backend-core
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [auth/auth.ts, auth/middleware.ts]
autonomous: true
requirements: [AUTH-03, AUTH-04, AUTH-05]
must_haves:
  truths:
    - "Owner can create cashier accounts with numeric PINs"
    - "Cashiers can log in using their numeric PIN"
    - "Cashiers cannot access owner-only endpoints"
  artifacts:
    - path: "auth/middleware.ts"
      provides: "Role enforcement logic"
    - path: "auth/auth.ts"
      provides: "Cashier management and PIN-login endpoints"
  key_links:
    - from: "auth/auth.ts"
      to: "auth/middleware.ts"
      via: "Encore API auth handlers"
---

<objective>
Implement RBAC, PIN-based login for Cashiers, and Role Enforcement Middleware.
Purpose: Secure the system and provide restricted access based on user roles (Owner vs Cashier).
Output: Cashier creation by Owners, PIN-based login for Cashiers, and middleware to enforce roles.
</objective>

<execution_context>
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-backend-core/01-CONTEXT.md
@.planning/phases/01-foundation-backend-core/01-RESEARCH.md
@.planning/phases/01-foundation-backend-core/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cashier creation and PIN-based login</name>
  <files>auth/auth.ts</files>
  <read_first>
    - auth/auth.ts
    - auth/user.entity.ts
  </read_first>
  <behavior>
    - Only Owners can create Cashier accounts via API.
    - Cashiers must have a numeric PIN.
    - Cashiers can login using only their PIN (or email + PIN as per standard PWA register flow).
    - PIN must be hashed before storage.
  </behavior>
  <action>
    Implement the following Encore API endpoints in `auth/auth.ts`:
    1. `POST /auth/users`: Allows Owners to create additional users (Owners or Cashiers). PIN is required for Cashiers.
    2. `POST /auth/pin-login`: Accepts a numeric PIN and authenticates a user (Cashier).
    3. Ensure PIN is hashed using a secure algorithm (like bcrypt or argon2).
  </action>
  <verify>
    <automated>encore test ./auth/...</automated>
  </verify>
  <acceptance_criteria>
    - `POST /auth/users` endpoint exists and requires Owner role (auth handler check)
    - `POST /auth/pin-login` correctly authenticates with valid PIN
    - Tests verify PIN hashing and authentication flow
  </acceptance_criteria>
  <done>Cashier creation and PIN-based login APIs are functional and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Role Enforcement Middleware</name>
  <files>auth/middleware.ts</files>
  <read_first>
    - auth/auth.ts
  </read_first>
  <action>
    Implement Encore middleware to enforce roles on endpoints:
    1. Create `auth/middleware.ts` to define Encore's authentication handler and middleware logic.
    2. Implement a `requireRole(role: Role)` function to restrict endpoint access.
    3. Update existing and future endpoints to use this middleware.
  </action>
  <verify>
    <automated>encore test ./auth/middleware.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Middleware correctly identifies user roles from the session/token.
    - Endpoints with `requireRole('OWNER')` reject 'CASHIER' users with 403 Forbidden.
  </acceptance_criteria>
  <done>Role-based access enforcement is active across the Auth service.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "Owner can create cashier accounts with numeric PINs"
    - "Cashiers can log in using their numeric PIN"
    - "Cashiers cannot access owner-only endpoints"
  artifacts:
    - path: "auth/middleware.ts"
      provides: "Role enforcement logic"
    - path: "auth/auth.ts"
      provides: "Cashier management and PIN-login endpoints"
  key_links:
    - from: "auth/auth.ts"
      to: "auth/middleware.ts"
      via: "Encore API auth handlers"
</must_haves>

<success_criteria>
Cashier login via PIN works, and Owners can manage users. Middleware correctly blocks unauthorized role access.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-backend-core/01-02-SUMMARY.md`
</output>
