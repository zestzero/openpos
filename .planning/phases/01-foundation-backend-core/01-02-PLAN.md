---
phase: 01-foundation-backend-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [db/queries/auth.sql, db/sqlc/auth.sql.go, internal/auth/service.go, internal/auth/handler.go, internal/middleware/auth.go]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]

must_haves:
  truths:
    - "Owner can register with email and password"
    - "Owner can login and receive a JWT"
    - "Cashier can login with a numeric PIN"
    - "Middleware correctly identifies user role from JWT"
  artifacts:
    - path: "internal/auth/service.go"
      provides: "Auth business logic"
    - path: "internal/middleware/auth.go"
      provides: "JWT validation and RBAC"
  key_links:
    - from: "internal/auth/handler.go"
      to: "internal/auth/service.go"
      via: "method call"
    - from: "internal/middleware/auth.go"
      to: "Context"
      via: "Setting user role/id"
---

<objective>
Implement the authentication system including Owner registration (email/password), Cashier login (PIN), and JWT-based authorization middleware with Role-Based Access Control (RBAC).

Purpose: Secure the application and establish identity for all subsequent operations.
Output: Working authentication endpoints and middleware to protect routes based on roles.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-backend-core/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Auth SQL queries and generate code</name>
  <files>db/queries/auth.sql, db/sqlc/models.go</files>
  <behavior>
    - Should be able to create a user with email, hashed password, and role.
    - Should be able to find a user by email.
    - Should be able to find a user by PIN (for cashiers).
    - Should be able to list cashiers created by an owner.
  </behavior>
  <action>
    Write SQL queries in `db/queries/auth.sql` for: `CreateUser`, `GetUserByEmail`, `GetUserByPIN`, and `ListCashiers`.
    Run `sqlc generate` to produce the Go database access code.
  </action>
  <verify>
    <automated>sqlc generate && ls db/sqlc/auth.sql.go</automated>
  </verify>
  <done>sqlc generated code for authentication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Auth Service and Handlers</name>
  <files>internal/auth/service.go, internal/auth/handler.go</files>
  <behavior>
    - POST /api/auth/register: Registers an owner, returns 201.
    - POST /api/auth/login: Validates credentials, returns JWT.
    - POST /api/auth/login/pin: Validates cashier PIN, returns JWT.
    - POST /api/auth/cashiers: (Owner only) Creates a new cashier account.
  </behavior>
  <action>
    Implement `AuthService` with `bcrypt` for password hashing.
    Implement HTTP handlers using `go-chi`.
    Use `golang-jwt/jwt/v5` for token generation.
  </action>
  <verify>
    <automated>go test ./internal/auth/...</automated>
  </verify>
  <done>Authentication business logic and endpoints implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Auth Middleware and RBAC</name>
  <files>internal/middleware/auth.go</files>
  <action>
    Implement JWT validation middleware that extracts the user ID and role from the token.
    Store user info in the request context.
    Implement a `RequireRole(role string)` middleware helper to enforce RBAC.
  </action>
  <verify>
    <automated>go test ./internal/middleware/...</automated>
  </verify>
  <done>Auth middleware and RBAC enforcement ready.</done>
</task>

</tasks>

<verification>
Use `curl` or a test script to:
1. Register an owner.
2. Login as owner to get JWT.
3. Use owner JWT to create a cashier.
4. Login as cashier with PIN to get cashier JWT.
5. Verify owner can access ERP-only route (protected by middleware) but cashier cannot.
</verification>

<success_criteria>
- Successful registration and login for Owners and Cashiers.
- JWT tokens correctly encode user ID and role.
- Middleware correctly rejects unauthorized requests with 401 or 403.
- Cashier creation is restricted to Owners.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-backend-core/01-02-SUMMARY.md`
</output>
