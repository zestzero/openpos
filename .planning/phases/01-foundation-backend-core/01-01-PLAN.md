---
phase: 01-foundation-backend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [auth/encore.service.ts, auth/migrations/1_create_users.up.sql, auth/user.entity.ts, auth/auth.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, PLAT-02, PLAT-03]
must_haves:
  truths:
    - "Owner can create an account with email and password"
    - "Owner can log in and receive an authentication token"
  artifacts:
    - path: "auth/migrations/1_create_users.up.sql"
      provides: "User table schema"
    - path: "auth/user.entity.ts"
      provides: "TypeORM User entity"
    - path: "auth/auth.ts"
      provides: "Registration and login API endpoints"
  key_links:
    - from: "auth/auth.ts"
      to: "auth/user.entity.ts"
      via: "TypeORM repository"
    - from: "auth/auth.ts"
      to: "auth/migrations/1_create_users.up.sql"
      via: "Database table schema"
---

<objective>
Setup the core Authentication service using Encore and TypeORM.
Purpose: Establish the identity and access control foundation for the entire POS/ERP system.
Output: Operational Auth service with Owner registration and login capabilities.
</objective>

<execution_context>
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-backend-core/01-CONTEXT.md
@.planning/phases/01-foundation-backend-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth service scaffold and User entity</name>
  <files>auth/encore.service.ts, auth/migrations/1_create_users.up.sql, auth/user.entity.ts</files>
  <read_first>
    - auth/encore.service.ts (if exists)
    - auth/user.entity.ts (if exists)
  </read_first>
  <action>
    1. Create `auth/encore.service.ts` to define the Encore service and its database.
    2. Create `auth/migrations/1_create_users.up.sql` with a `users` table including: `id` (UUID PK), `email` (unique), `password_hash`, `role` (enum: 'OWNER', 'CASHIER'), `pin_hash`, `is_active`, `created_at`, `updated_at`.
    3. Create `auth/user.entity.ts` using TypeORM to map the `User` entity to the `users` table. Ensure `synchronize: false` is respected globally (handled via Encore integration).
  </action>
  <verify>
    <automated>encore db shell auth --query "SELECT * FROM users LIMIT 1;"</automated>
  </verify>
  <acceptance_criteria>
    - `auth/migrations/1_create_users.up.sql` exists and contains `CREATE TABLE users`
    - `auth/user.entity.ts` defines `User` class with correct TypeORM decorators
    - `encore db shell auth` command succeeds (indicating DB is provisioned)
  </acceptance_criteria>
  <done>Auth service is scaffolded and User table exists in database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Owner registration and login</name>
  <files>auth/auth.ts</files>
  <read_first>
    - auth/user.entity.ts
  </read_first>
  <behavior>
    - Register: Valid email and password creates a user with 'OWNER' role and returns success.
    - Login: Valid email and password returns a token/session; invalid credentials return 401.
    - Role Check: First user registered should default to 'OWNER'.
  </behavior>
  <action>
    Implement the following Encore API endpoints in `auth/auth.ts`:
    1. `POST /auth/register`: Accepts email/password, hashes password using bcrypt, saves user as 'OWNER'.
    2. `POST /auth/login`: Validates credentials, returns a session token (using Encore's built-in auth or a simple JWT/cookie approach as per research).
    3. Ensure password hashing is secure and never stored in plain text.
  </action>
  <verify>
    <automated>encore test ./auth/...</automated>
  </verify>
  <acceptance_criteria>
    - `auth/auth.ts` has `register` and `login` endpoints marked as `//encore:api`
    - Tests verify successful registration and login
    - Tests verify 401 for incorrect password
  </acceptance_criteria>
  <done>Owner registration and login APIs are functional and tested.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "Owner can create an account with email and password"
    - "Owner can log in and receive an authentication token"
  artifacts:
    - path: "auth/migrations/1_create_users.up.sql"
      provides: "User table schema"
    - path: "auth/user.entity.ts"
      provides: "TypeORM User entity"
    - path: "auth/auth.ts"
      provides: "Registration and login API endpoints"
  key_links:
    - from: "auth/auth.ts"
      to: "auth/user.entity.ts"
      via: "TypeORM repository"
    - from: "auth/auth.ts"
      to: "auth/migrations/1_create_users.up.sql"
      via: "Database table schema"
</must_haves>

<success_criteria>
Registration and Login endpoints return 200 OK for valid requests and data is persisted in the 'auth' database.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-backend-core/01-01-SUMMARY.md`
</output>
