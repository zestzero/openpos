---
phase: 01-foundation-backend-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docker-compose.yml, db/migrations/000001_init.up.sql, db/migrations/000001_init.down.sql, cmd/server/main.go, go.mod, sqlc.yaml]
autonomous: true
requirements: [PLAT-02, PLAT-03]

must_haves:
  truths:
    - "Docker Compose starts PostgreSQL and the Go application"
    - "Database migrations are automatically applied on startup"
    - "Go server listens for HTTP requests on port 8080"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Infrastructure orchestration"
    - path: "db/migrations/000001_init.up.sql"
      provides: "Initial database schema"
    - path: "cmd/server/main.go"
      provides: "Application entry point"
  key_links:
    - from: "cmd/server/main.go"
      to: "PostgreSQL"
      via: "pgxpool connection"
      pattern: "pgxpool\\.New"
---

<objective>
Setup the foundational infrastructure for the Go backend, including Docker Compose for local development, golang-migrate for schema management, and the initial chi router structure.

Purpose: Provide a stable, reproducible development environment and database workflow.
Output: Operational Go backend container connected to a PostgreSQL database with managed migrations.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/stack.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and setup Docker Compose</name>
  <files>go.mod, docker-compose.yml, Dockerfile</files>
  <action>
    Initialize the Go module as `github.com/zestzero/openpos`.
    Create a `docker-compose.yml` that defines a `db` service (PostgreSQL 16) and an `app` service (Go).
    Create a multi-stage `Dockerfile` for the Go application.
    Ensure the `app` service depends on `db` and uses environment variables for database connection (DATABASE_URL).
  </action>
  <verify>
    <automated>docker compose config && ls go.mod</automated>
  </verify>
  <done>Go module initialized and docker-compose.yml defined.</done>
</task>

<task type="auto">
  <name>Task 2: Setup database migrations and sqlc</name>
  <files>db/migrations/000001_init.up.sql, db/migrations/000001_init.down.sql, sqlc.yaml</files>
  <action>
    Create the first migration file `db/migrations/000001_init.up.sql` with initial table definitions: `users`, `categories`, `products`, `variants`, and `inventory_ledger`. 
    Include UUID extensions and standard timestamps (created_at, updated_at).
    Configure `sqlc.yaml` to point to `db/queries` and `db/migrations`, targeting `pgx/v5`.
  </action>
  <verify>
    <automated>ls db/migrations/*.sql sqlc.yaml</automated>
  </verify>
  <done>Initial migration and sqlc configuration created.</done>
</task>

<task type="auto">
  <name>Task 3: Implement core server and database connection</name>
  <files>cmd/server/main.go, internal/database/db.go</files>
  <action>
    Implement `cmd/server/main.go` using `go-chi/chi/v5`.
    Setup a `pgxpool` connection to the database.
    Integrate `golang-migrate` to run migrations on startup within the Go code.
    Add a simple `/health` endpoint to verify the server is running.
  </action>
  <verify>
    <automated>go build ./cmd/server && go test ./...</automated>
  </verify>
  <done>Go server starts, runs migrations, and connects to PG.</done>
</task>

</tasks>

<verification>
Run `docker compose up --build -d` and check logs to ensure migrations ran successfully and the server is listening.
`curl http://localhost:8080/health` should return 200 OK.
</verification>

<success_criteria>
- `docker compose up` successfully starts all services.
- `golang-migrate` correctly applies the initial schema to PostgreSQL.
- Go application establishes a connection pool to PostgreSQL via `pgxpool`.
- Health check endpoint is accessible.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-backend-core/01-01-SUMMARY.md`
</output>
