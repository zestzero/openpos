---
phase: 01-foundation-backend-core
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [db/queries/catalog.sql, db/sqlc/catalog.sql.go, internal/catalog/service.go, internal/catalog/handler.go]
autonomous: true
requirements: [PLAT-02]

must_haves:
  truths:
    - "Products can be created with categories"
    - "Variants can be created linked to a parent product"
    - "Variants have unique SKUs and barcodes"
    - "Catalog can be queried by category"
  artifacts:
    - path: "internal/catalog/service.go"
      provides: "Catalog business logic"
    - path: "db/queries/catalog.sql"
      provides: "SQL queries for products and variants"
  key_links:
    - from: "internal/catalog/handler.go"
      to: "internal/catalog/service.go"
      via: "method call"
---

<objective>
Implement the core data model and backend services for the Product Catalog, following the Product-Variant hierarchy.

Purpose: Provide the structured data layer for product management and POS consumption.
Output: API endpoints for managing categories, products, and variants.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/SUMMARY.md
@.planning/phases/01-foundation-backend-core/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Category and Product queries</name>
  <files>db/queries/catalog.sql, db/sqlc/models.go</files>
  <behavior>
    - Can create, list, and update categories.
    - Can create a product linked to a category.
    - Can create variants for a product with SKU, barcode, price, cost.
    - Can list products with their variants.
  </behavior>
  <action>
    Define SQL queries in `db/queries/catalog.sql` for categories, products, and variants.
    Ensure `variants` table has a foreign key to `products`.
    Run `sqlc generate`.
  </action>
  <verify>
    <automated>sqlc generate && ls db/sqlc/catalog.sql.go</automated>
  </verify>
  <done>Database layer for catalog implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Catalog Service logic</name>
  <files>internal/catalog/service.go</files>
  <behavior>
    - CreateProduct should also handle initial variant creation if provided.
    - GetProduct should return the product and all its variants.
    - Prevent duplicate SKUs or barcodes across all variants.
  </behavior>
  <action>
    Implement business logic in `CatalogService`.
    Handle transactions when creating products and multiple variants at once.
    Include validation for barcodes and prices.
  </action>
  <verify>
    <automated>go test ./internal/catalog/...</automated>
  </verify>
  <done>Catalog service logic with validation and transactions ready.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Catalog Handlers</name>
  <files>internal/catalog/handler.go</files>
  <action>
    Implement HTTP handlers for:
    - GET/POST /api/catalog/categories
    - GET/POST /api/catalog/products
    - GET /api/catalog/products/{id}
    - GET /api/catalog/variants/search?q={barcode|sku}
  </action>
  <verify>
    <automated>go build ./cmd/server</automated>
  </verify>
  <done>Catalog API endpoints available.</done>
</task>

</tasks>

<verification>
Verify via API:
1. Create a "Beverages" category.
2. Create a "Singha Beer" product in that category.
3. Add "330ml Can" and "620ml Bottle" variants with different barcodes and prices.
4. Search for the variant by barcode and ensure it returns the correct price.
</verification>

<success_criteria>
- Product-Variant hierarchy is strictly enforced in the database.
- Unique constraints on SKU and barcode are functional.
- API returns nested product/variant data correctly.
- Searching by barcode/SKU returns the specific variant.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-backend-core/01-03-SUMMARY.md`
</output>
