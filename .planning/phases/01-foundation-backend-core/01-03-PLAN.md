---
phase: 01-foundation-backend-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [catalog/encore.service.ts, catalog/migrations/1_create_catalog.up.sql, catalog/entities.ts, catalog/api.ts]
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PLAT-02, PLAT-03]
must_haves:
  truths:
    - "Owner can create products with variants and categories"
    - "System enforces unique SKUs and barcodes"
    - "Products can be assigned to categories"
  artifacts:
    - path: "catalog/migrations/1_create_catalog.up.sql"
      provides: "Catalog database schema"
    - path: "catalog/entities.ts"
      provides: "TypeORM entities for Catalog"
    - path: "catalog/api.ts"
      provides: "CRUD endpoints for Product/Variant/Category"
  key_links:
    - from: "catalog/api.ts"
      to: "catalog/entities.ts"
      via: "TypeORM repository"
    - from: "catalog/api.ts"
      to: "auth/middleware.ts"
      via: "Role-based access checks"
---

<objective>
Setup the core Catalog service for products, variants, and categories.
Purpose: Create the central inventory catalog system for the POS and ERP.
Output: Operational Catalog service with Product, Variant, and Category data models and CRUD endpoints.
</objective>

<execution_context>
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-backend-core/01-CONTEXT.md
@.planning/phases/01-foundation-backend-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Catalog service scaffold and schema</name>
  <files>catalog/encore.service.ts, catalog/migrations/1_create_catalog.up.sql, catalog/entities.ts</files>
  <read_first>
    - catalog/encore.service.ts (if exists)
    - catalog/entities.ts (if exists)
  </read_first>
  <action>
    1. Create `catalog/encore.service.ts` defining the catalog service and database.
    2. Create `catalog/migrations/1_create_catalog.up.sql` with tables: `categories` (id, name, order), `products` (id, name, description, category_id), `variants` (id, product_id, sku, barcode, price_cents, cost_cents, active).
    3. Create `catalog/entities.ts` with TypeORM entities for `Category`, `Product`, and `Variant` mapping to the migrations.
  </action>
  <verify>
    <automated>encore db shell catalog --query "SELECT * FROM products LIMIT 1;"</automated>
  </verify>
  <acceptance_criteria>
    - `catalog/migrations/1_create_catalog.up.sql` exists and contains correct `CREATE TABLE` statements
    - `catalog/entities.ts` defines all three entities with correct relationships (Product -> Category, Product -> Variant)
    - `encore db shell catalog` command succeeds
  </acceptance_criteria>
  <done>Catalog service is scaffolded and data schema is established in the database.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Catalog CRUD endpoints</name>
  <files>catalog/api.ts</files>
  <read_first>
    - catalog/entities.ts
  </read_first>
  <behavior>
    - Categories: Create, List, Edit, Reorder (by order field).
    - Products: Create, List, Edit, Archive.
    - Variants: Create (linked to Product), List, Edit, Archive.
    - Uniqueness: SKUs and Barcodes must be unique across all variants.
  </behavior>
  <action>
    Implement Encore API endpoints in `catalog/api.ts`:
    1. `POST /catalog/categories`, `GET /catalog/categories`
    2. `POST /catalog/products`, `GET /catalog/products`, `PATCH /catalog/products/:id`
    3. `POST /catalog/variants`, `PATCH /catalog/variants/:id`
    4. Ensure role-based access is applied via the Auth service's middleware (sharing logic as needed).
  </action>
  <verify>
    <automated>encore test ./catalog/...</automated>
  </verify>
  <acceptance_criteria>
    - CRUD endpoints for all three entities are functional
    - SKUs and Barcodes are verified for uniqueness during creation/update
    - Tests verify endpoint logic and database persistence
  </acceptance_criteria>
  <done>Catalog CRUD APIs are functional and tested.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "Owner can create products with variants and categories"
    - "System enforces unique SKUs and barcodes"
    - "Products can be assigned to categories"
  artifacts:
    - path: "catalog/migrations/1_create_catalog.up.sql"
      provides: "Catalog database schema"
    - path: "catalog/entities.ts"
      provides: "TypeORM entities for Catalog"
    - path: "catalog/api.ts"
      provides: "CRUD endpoints for Product/Variant/Category"
  key_links:
    - from: "catalog/api.ts"
      to: "catalog/entities.ts"
      via: "TypeORM repository"
    - from: "catalog/api.ts"
      to: "auth/middleware.ts"
      via: "Role-based access checks"
</must_haves>

<success_criteria>
Product and Variant entities are correctly persisted, and CRUD endpoints return expected results for valid requests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-backend-core/01-03-SUMMARY.md`
</output>
