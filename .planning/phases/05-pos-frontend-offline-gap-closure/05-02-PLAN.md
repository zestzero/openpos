---
phase: 05-pos-frontend-offline-gap-closure
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/routes/pos.tsx
  - frontend/src/routes/pos.catalog.tsx
  - frontend/src/routes/pos.scan.tsx
  - frontend/src/pos/layout/PosLayout.tsx
  - frontend/src/routes/__tests__/pos-shell.test.tsx
autonomous: true
requirements: [POS-01, POS-02, POS-03, POS-04, POS-05, POS-06, POS-07, PLAT-01, PLAT-04]
must_haves:
  truths:
    - "The cashier still reaches the main POS, catalog, and scan entry points through separate route modules"
    - "The POS shell keeps the mobile-first header, banner, and bottom-cart affordance intact"
    - "The shell regression test proves search, catalog browsing, barcode scanning, quick keys, and cart wiring are still present"
  artifacts:
    - path: "frontend/src/routes/pos.tsx"
      provides: "Main cashier floor route"
    - path: "frontend/src/routes/pos.catalog.tsx"
      provides: "Catalog browsing route"
    - path: "frontend/src/routes/pos.scan.tsx"
      provides: "Barcode scanning route"
    - path: "frontend/src/pos/layout/PosLayout.tsx"
      provides: "Shared mobile-first shell"
    - path: "frontend/src/routes/__tests__/pos-shell.test.tsx"
      provides: "POS shell smoke coverage"
  key_links:
    - from: "frontend/src/routes/pos.tsx"
      to: "frontend/src/pos/layout/PosLayout.tsx"
      via: "shared shell wrapper"
      pattern: "<PosLayout>"
    - from: "frontend/src/routes/pos.catalog.tsx"
      to: "frontend/src/pos/components/CatalogGrid.tsx"
      via: "catalog browsing pane"
      pattern: "CatalogGrid"
    - from: "frontend/src/routes/pos.scan.tsx"
      to: "frontend/src/pos/components/BarcodeScanner.tsx"
      via: "scanner lane and wedge controls"
      pattern: "BarcodeScanner|useKeyboardWedge"
    - from: "frontend/src/pos/layout/PosLayout.tsx"
      to: "frontend/src/pos/components/OfflineBanner.tsx"
      via: "offline banner above the main content"
      pattern: "OfflineBanner"
---

<objective>
Keep the cashier-facing POS route tree verifiable after the offline-sync repair.

Purpose: close the remaining phase-02 audit gaps by proving the separate cashier pages still render the expected shell and scan/catalog controls.
Output: named route exports, shared online/offline source, and a POS shell smoke test.
</objective>

<execution_context>
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/kritbannachaisirisuk/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-pos-frontend-offline/02-01-SUMMARY.md
@.planning/phases/02-pos-frontend-offline/02-02-SUMMARY.md
@.planning/phases/02-pos-frontend-offline/02-05-SUMMARY.md
@AGENTS.md
@frontend/src/routes/pos.tsx
@frontend/src/routes/pos.catalog.tsx
@frontend/src/routes/pos.scan.tsx
@frontend/src/pos/layout/PosLayout.tsx
@frontend/src/pos/components/PosHeader.tsx
@frontend/src/pos/components/OfflineBanner.tsx
@frontend/src/pos/components/SyncStatus.tsx

<interfaces>
From `frontend/src/routes/pos.tsx`:
```tsx
export function PosRoute()
```

From `frontend/src/routes/pos.catalog.tsx`:
```tsx
export function PosCatalogRoute()
```

From `frontend/src/routes/pos.scan.tsx`:
```tsx
export function ScanPage()
```

From `frontend/src/pos/layout/PosLayout.tsx`:
```tsx
interface PosLayoutProps {
  children?: ReactNode
}
```

From `frontend/src/pos/components/PosHeader.tsx`:
```tsx
interface PosHeaderProps {
  user: AuthUser | null
  online: boolean
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the cashier route components and render quick keys</name>
  <files>frontend/src/routes/pos.tsx, frontend/src/routes/pos.catalog.tsx, frontend/src/routes/pos.scan.tsx, frontend/src/pos/layout/PosLayout.tsx</files>
  <read_first>
    Read `frontend/src/routes/pos.tsx`, `frontend/src/routes/pos.catalog.tsx`, `frontend/src/routes/pos.scan.tsx`, `frontend/src/pos/layout/PosLayout.tsx`, `frontend/src/pos/components/PosHeader.tsx`, `frontend/src/pos/components/OfflineBanner.tsx`, `frontend/src/pos/components/SyncStatus.tsx`, `.planning/phases/02-pos-frontend-offline/02-01-SUMMARY.md`, and `.planning/phases/02-pos-frontend-offline/02-05-SUMMARY.md` before editing.
  </read_first>
  <action>
    Export the inner page components as named functions so tests can render them directly:
    - `PosRoute` from `frontend/src/routes/pos.tsx`
    - `PosCatalogRoute` from `frontend/src/routes/pos.catalog.tsx`
    - `ScanPage` from `frontend/src/routes/pos.scan.tsx`

    In `PosRoute`, render the existing `QuickKeysBar` between the search summary and the catalog/cart grid so the quick-key requirement is actually visible on the cashier floor. At the same time, replace the private `useOnlineStatus()` helper in `PosLayout.tsx` with the shared `useNetworkStatus()` hook so the shell reads one online/offline source. Keep the route paths, layout spacing, sticky header, and bottom cart anchor unchanged.
  </action>
  <acceptance_criteria>
    - `grep -n "export function PosRoute\|export function PosCatalogRoute\|export function ScanPage\|useNetworkStatus\|QuickKeysBar" frontend/src/routes/pos.tsx frontend/src/routes/pos.catalog.tsx frontend/src/routes/pos.scan.tsx frontend/src/pos/layout/PosLayout.tsx` finds the exported page components, shared hook, and quick-key bar.
    - `cd frontend && npm run build` still succeeds after the route exports and shell refactor.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm run build</automated>
  </verify>
  <done>The cashier route tree is testable without changing its visible structure.</done>
</task>

<task type="auto">
  <name>Task 2: Add a POS shell smoke test for all cashier pages</name>
  <files>frontend/src/routes/__tests__/pos-shell.test.tsx</files>
  <read_first>
    Read `frontend/src/routes/pos.tsx`, `frontend/src/routes/pos.catalog.tsx`, `frontend/src/routes/pos.scan.tsx`, `frontend/src/routes/__tests__/erp-shell.test.tsx`, and `frontend/src/test/setup.ts` before writing the smoke test.
  </read_first>
  <action>
    Create a Vitest smoke suite that renders the exported cashier page components with mocked auth/cart/network hooks and proves the shell still exposes the expected cashier surfaces:
    1. `PosRoute` shows search, category navigation, the quick-key bar, the cart panel, and the floating cart anchor.
    2. `PosCatalogRoute` shows the category rail and catalog grid.
    3. `ScanPage` shows the scanner lane, scanner controls, and the keyboard-wedge state panel.

    Mock `useAuth`, `useCart`, `useFavorites`, `useNetworkStatus`, `useKeyboardWedge`, and the scanner API/component so the test stays deterministic. Keep the assertions on visible text/roles only; do not snapshot the layout or mock the whole router tree.
  </action>
  <acceptance_criteria>
    - `grep -n "PosRoute\|PosCatalogRoute\|ScanPage\|QuickKeysBar\|CatalogGrid\|BarcodeScanner" frontend/src/routes/__tests__/pos-shell.test.tsx` finds all three smoke cases.
    - `cd frontend && npm exec vitest run src/routes/__tests__/pos-shell.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/routes/__tests__/pos-shell.test.tsx</automated>
  </verify>
  <done>The POS shell is regression-covered across the main cashier pages.</done>
</task>

</tasks>

<verification>
The cashier shell, catalog route, and scan route all remain reachable and covered by a focused smoke test.
</verification>

<success_criteria>
- The cashier route components are exported for direct test coverage.
- The main POS route once again exposes quick keys on the selling floor.
- The catalog and scan pages remain visible through their dedicated route modules.
- The POS shell smoke test passes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pos-frontend-offline-gap-closure/05-02-SUMMARY.md`
</output>
