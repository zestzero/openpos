# Deferred Items

- `frontend/src/routes/pos.tsx` has an unrelated `QuickKeysBar` unused import error (`TS6133`) that blocks a full frontend build.
- Found during verification for plan 05-01.
- Not fixed here because it is outside the offline sync contract scope.
