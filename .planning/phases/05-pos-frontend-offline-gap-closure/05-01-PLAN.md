---
phase: 05-pos-frontend-offline-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pos/hooks/syncContract.ts
  - frontend/src/pos/hooks/useOfflineOrders.ts
  - frontend/src/pos/hooks/useSync.ts
  - frontend/src/pos/__tests__/syncContract.test.ts
autonomous: true
requirements: [OFF-01, OFF-02, OFF-03, OFF-04]
must_haves:
  truths:
    - "Queued offline orders are serialized with the backend's client_uuid sync contract"
    - "Failed syncs keep the correct queued order in the retry queue"
    - "Pending and syncing counts in Dexie match the actual queued-order statuses"
  artifacts:
    - path: "frontend/src/pos/hooks/syncContract.ts"
      provides: "Shared offline sync contract helpers"
    - path: "frontend/src/pos/hooks/useOfflineOrders.ts"
      provides: "Dexie queue + sync-state updates"
    - path: "frontend/src/pos/hooks/useSync.ts"
      provides: "Batch sync and retry flow"
    - path: "frontend/src/pos/__tests__/syncContract.test.ts"
      provides: "Regression coverage for sync payloads and retry math"
  key_links:
    - from: "frontend/src/pos/hooks/useSync.ts"
      to: "frontend/src/pos/hooks/syncContract.ts"
      via: "buildSyncOrdersRequest and collectFailedClientUUIDs"
      pattern: "client_uuid|variant_id|unit_price"
    - from: "frontend/src/pos/hooks/useOfflineOrders.ts"
      to: "frontend/src/pos/hooks/syncContract.ts"
      via: "deriveSyncSnapshot"
      pattern: "pendingCount|isSyncing"
---

<objective>
Repair the offline order sync contract so queued orders, retry state, and backend responses agree.

Purpose: prevent offline checkouts from disappearing or being retried under the wrong ID.
Output: shared sync-contract helper, wired hooks, and regression tests.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-pos-frontend-offline/02-02-SUMMARY.md
@.planning/phases/02-pos-frontend-offline/02-05-SUMMARY.md
@AGENTS.md
@frontend/src/lib/db.ts
@frontend/src/pos/hooks/useOfflineOrders.ts
@frontend/src/pos/hooks/useSync.ts
@internal/sales/service.go

<interfaces>
From `frontend/src/lib/db.ts`:
```ts
export interface QueuedOrder {
  id: string
  userId: string
  status: 'pending' | 'syncing' | 'failed'
  items: Array<{ variantId: string; quantity: number; priceSnapshot: number }>
  total: number
  createdAt: number
  retryCount: number
  lastError?: string
}

export interface SyncState {
  id: 'default'
  lastSyncAt?: number
  isSyncing: boolean
  pendingCount: number
}
```

From `internal/sales/service.go`:
```go
type SyncResult struct {
  Processed int         `json:"processed"`
  Succeeded int         `json:"succeeded"`
  Failed    int         `json:"failed"`
  Errors    []SyncError `json:"errors"`
}

type SyncError struct {
  ClientUUID string `json:"client_uuid"`
  Error      string `json:"error"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the shared offline-sync contract</name>
  <files>frontend/src/pos/hooks/syncContract.ts, frontend/src/pos/__tests__/syncContract.test.ts</files>
  <read_first>
    Read `frontend/src/lib/db.ts`, `internal/sales/service.go`, `frontend/src/pos/hooks/useSync.ts`, `frontend/src/pos/hooks/useOfflineOrders.ts`, `.planning/phases/02-pos-frontend-offline/02-02-SUMMARY.md`, and `.planning/phases/02-pos-frontend-offline/02-05-SUMMARY.md` before editing.
  </read_first>
  <behavior>
    - `buildSyncOrdersRequest()` maps queued orders to the backend payload with `client_uuid`, `variant_id`, `quantity`, and `unit_price`.
    - `collectFailedClientUUIDs()` indexes backend errors by `client_uuid`.
    - `deriveSyncSnapshot()` reports `pendingCount` and `isSyncing` from queued-order statuses.
    - `getNextRetryDelayMs()` returns 2000ms, doubles on each retry, and caps at 60000ms.
  </behavior>
  <action>
    Create a shared contract helper that both offline hooks can import instead of hard-coding the sync payload and retry math. Keep the field names exactly aligned with the backend's `client_uuid` error contract from `internal/sales/service.go`; do not introduce a second `order_id` alias.
  </action>
  <acceptance_criteria>
    - `grep -n "client_uuid\|variant_id\|unit_price\|getNextRetryDelayMs" frontend/src/pos/hooks/syncContract.ts frontend/src/pos/__tests__/syncContract.test.ts` finds the contract code.
    - `cd frontend && npm exec vitest run src/pos/__tests__/syncContract.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/pos/__tests__/syncContract.test.ts</automated>
  </verify>
  <done>The sync contract is expressed once and covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire offline queue state to the shared contract</name>
  <files>frontend/src/pos/hooks/useOfflineOrders.ts, frontend/src/pos/hooks/useSync.ts</files>
  <read_first>
    Read `frontend/src/pos/hooks/syncContract.ts`, `frontend/src/pos/hooks/useOfflineOrders.ts`, `frontend/src/pos/hooks/useSync.ts`, `frontend/src/lib/db.ts`, and `internal/sales/service_test.go` before editing.
  </read_first>
  <action>
    Import the new sync helpers into both hooks. Update `useOfflineOrders()` so every queue mutation refreshes `syncState.pendingCount` and `syncState.isSyncing` from the actual Dexie rows, not from stale local assumptions. Update `useSync()` so it:
    1. builds the POST body with `client_uuid` keys,
    2. treats backend errors as `client_uuid` lookups,
    3. retries the exact orders that were marked syncing if the request throws,
    4. records `lastSyncAt` after a successful batch,
    5. uses the shared retry-delay helper instead of inline math.
    Do not change the route contract or introduce new persistence tables.
  </action>
  <acceptance_criteria>
    - `grep -n "buildSyncOrdersRequest\|collectFailedClientUUIDs\|deriveSyncSnapshot\|lastSyncAt" frontend/src/pos/hooks/useOfflineOrders.ts frontend/src/pos/hooks/useSync.ts` finds the wiring.
    - `go test ./internal/sales -run TestSyncOrdersProcessesSequentially -count=1` passes, proving the backend sync error contract remains keyed by `client_uuid`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/pos/__tests__/syncContract.test.ts &amp;&amp; cd .. &amp;&amp; go test ./internal/sales -run TestSyncOrdersProcessesSequentially -count=1</automated>
  </verify>
  <done>Queued offline orders keep the right retry identity and sync counters stay truthful.</done>
</task>

</tasks>

<verification>
Offline queueing, retry identity, and sync-state bookkeeping all line up with the backend contract.
</verification>

<success_criteria>
- Offline orders serialize with the backend's `client_uuid` contract.
- Sync failures are attributed to the correct queued order and remain retryable.
- Dexie sync counters reflect real queued statuses after queue mutations and sync runs.
- Contract regression tests and backend sync tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-pos-frontend-offline-gap-closure/05-01-SUMMARY.md`
</output>
