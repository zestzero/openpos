---
phase: 08-receipt-replay-reopen-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/constants.ts
  - frontend/src/pos/hooks/useLatestReceipt.ts
  - frontend/src/pos/components/LatestReceiptReprint.tsx
  - frontend/src/pos/components/CartPanel.tsx
  - frontend/src/routes/pos.tsx
  - frontend/src/routes/__tests__/pos-receipt-replay.test.tsx
autonomous: true
requirements:
  - REC-03
must_haves:
  truths:
    - "Cashier can complete a sale and immediately see a lightweight Reprint receipt action for the latest paid order."
    - "Cashier can reprint the latest receipt only while online, and replay fetches GET /api/orders/{id}/receipt as source of truth."
    - "Cashier sees a clear non-destructive error if the persisted receipt fetch fails or the order is missing."
    - "The POS surface does not add a recent-orders list, order editing, or Reopen sale wording."
  artifacts:
    - path: "frontend/src/pos/hooks/useLatestReceipt.ts"
      provides: "localStorage-backed latest receipt id and endpoint-backed replay action"
      exports: ["useLatestReceipt"]
    - path: "frontend/src/pos/components/LatestReceiptReprint.tsx"
      provides: "cashier-facing Reprint receipt action with online/offline/error states"
      exports: ["LatestReceiptReprint"]
    - path: "frontend/src/pos/components/CartPanel.tsx"
      provides: "successful checkout records the persisted order id for replay"
      contains: "rememberLatestReceipt"
    - path: "frontend/src/routes/pos.tsx"
      provides: "lightweight latest receipt affordance on POS surface"
      contains: "LatestReceiptReprint"
    - path: "frontend/src/routes/__tests__/pos-receipt-replay.test.tsx"
      provides: "regression coverage for persisted receipt endpoint replay, offline disable, and failure copy"
  key_links:
    - from: "frontend/src/pos/components/CartPanel.tsx"
      to: "frontend/src/pos/hooks/useLatestReceipt.ts"
      via: "stores created.data.id after successful api.completePayment"
      pattern: "rememberLatestReceipt\(created\.data\.id\)"
    - from: "frontend/src/pos/hooks/useLatestReceipt.ts"
      to: "frontend/src/lib/api.ts"
      via: "api.getReceipt(orderId)"
      pattern: "api\.getReceipt\(latestReceiptId\)"
    - from: "frontend/src/pos/hooks/useLatestReceipt.ts"
      to: "frontend/src/lib/receipt.ts"
      via: "printReceipt(result.data) after backend fetch"
      pattern: "printReceipt\(result\.data\)"
    - from: "frontend/src/routes/pos.tsx"
      to: "frontend/src/pos/components/LatestReceiptReprint.tsx"
      via: "renders latest receipt action without recent-order lookup"
      pattern: "<LatestReceiptReprint"
---

<objective>
Wire persisted receipt lookup into the POS replay/reprint flow.

Purpose: Close the REC-03 audit blocker by proving receipt replay consumes the existing backend `GET /api/orders/{id}/receipt` endpoint instead of relying on the payment-completion snapshot alone.
Output: A tested latest-receipt reprint action in the POS surface, backed by the persisted receipt endpoint and disabled offline.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-receipt-replay-reopen-closure/08-CONTEXT.md
@.planning/phases/03-payments-receipts/03-01-SUMMARY.md
@.planning/phases/05-pos-frontend-offline-gap-closure/05-02-SUMMARY.md
@.planning/phases/06-payments-receipts-sale-finalization/06-02-SUMMARY.md
@frontend/src/lib/api.ts
@frontend/src/lib/constants.ts
@frontend/src/lib/receipt.ts
@frontend/src/pos/components/CartPanel.tsx
@frontend/src/routes/pos.tsx
@frontend/src/pos/hooks/useNetworkStatus.ts

<interfaces>
<!-- Key contracts executor should use directly. Do not rediscover these before implementation. -->

From `frontend/src/lib/api.ts`:
```typescript
export interface ApiSuccess<T> { data: T }
export type PaymentMethod = 'cash' | 'promptpay'
export interface ReceiptSnapshot {
  store_name: string
  paid_at: string
  order_id: string
  items: ReceiptItem[]
  discount_amount: number
  total_amount: number
  payment_method: PaymentMethod
  tendered_amount: number
  change_due: number
}
export const api = {
  completePayment(orderId: string, body: CompletePaymentRequest): Promise<ApiSuccess<ReceiptSnapshot>>,
  getReceipt(orderId: string): Promise<ApiSuccess<ReceiptSnapshot>>,
  createOrder(orderData: CreateOrderRequest): Promise<ApiSuccess<OrderResponse>>,
}
```

From `frontend/src/lib/receipt.ts`:
```typescript
export function printReceipt(snapshot: ReceiptSnapshot): Promise<void>
export function buildReceiptText(snapshot: ReceiptSnapshot): string
```

From `frontend/src/pos/hooks/useNetworkStatus.ts`:
```typescript
export function useNetworkStatus(): { isOnline: boolean }
```

From `frontend/src/pos/hooks/usePosCheckoutSession.ts`:
```typescript
export interface PosCheckoutSession {
  orderId: string
  stage: 'building' | 'review' | 'payment'
  discountAmount: number
  paymentMethod: PaymentMethod
  tenderedAmount: number
  updatedAt: number
}
```
</interfaces>

<locked_decisions>
- D-01: Provide receipt replay from checkout success path and keep a lightweight latest-receipt reopen/reprint action in the POS surface.
- D-02: Do not add a full recent-orders lookup/list.
- D-03: Persisted receipt replay/reprint must fetch `GET /api/orders/{id}/receipt` and treat backend receipt as source of truth.
- D-04: Tests must explicitly prove frontend consumes the existing backend receipt endpoint for replay/reprint.
- D-05: Disable receipt replay/reprint while offline.
- D-06: Fetch failure or missing order shows clear non-destructive error and keeps cashier in current POS state.
- D-07: Fresh checkout printing may keep using current payment-completion flow.
- D-08: Cashier-facing action label is “Reprint receipt”.
- D-09: Avoid “Reopen sale” wording.
</locked_decisions>

<deferred_ideas>
- Do not implement full recent-orders lookup/list.
- Do not implement sale editing, resuming, or any “Reopen sale” behavior/wording.
- Do not add digital receipts, customer history, discounts, or new payment capabilities.
</deferred_ideas>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the latest-receipt replay contract and regression tests</name>
  <files>frontend/src/lib/constants.ts, frontend/src/pos/hooks/useLatestReceipt.ts, frontend/src/routes/__tests__/pos-receipt-replay.test.tsx</files>
  <behavior>
    - Test 1: when a latest receipt id exists and the cashier clicks “Reprint receipt”, `api.getReceipt(latestReceiptId)` is called before `printReceipt(result.data)` per D-03 and D-04.
    - Test 2: when `useNetworkStatus()` reports offline, the “Reprint receipt” button is disabled and no endpoint call is made per D-05.
    - Test 3: when `api.getReceipt()` rejects or returns missing-order error, the UI shows clear non-destructive copy and remains on the POS route per D-06.
    - Test 4: the rendered UI never contains “Reopen sale” and does not render a recent-orders list per D-02 and D-09.
  </behavior>
  <action>Create `STORAGE_KEY_LATEST_RECEIPT` in `frontend/src/lib/constants.ts`. Create `frontend/src/pos/hooks/useLatestReceipt.ts` exporting `useLatestReceipt()`, with state initialized from localStorage, `rememberLatestReceipt(orderId: string)`, `clearLatestReceipt()`, and `reprintLatestReceipt()` that calls `api.getReceipt(latestReceiptId)` then `printReceipt(result.data)`. Guard storage access with try/catch like `usePosCheckoutSession`. Keep replay online-only by returning disabled/error state when `isOnline` is false, not by attempting an offline replay, per D-05. Write `frontend/src/routes/__tests__/pos-receipt-replay.test.tsx` first using Vitest and React Testing Library with mocked `api.getReceipt`, `printReceipt`, `useNetworkStatus`, and localStorage. Use behavior-focused tests and role/text queries. Do not add an order-history fixture or full list, per D-02.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/routes/__tests__/pos-receipt-replay.test.tsx</automated>
  </verify>
  <done>The test file exists, `useLatestReceipt` exports the replay contract, tests prove endpoint-backed replay and offline/error guards, and the targeted Vitest command passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the cashier-facing Reprint receipt action</name>
  <files>frontend/src/pos/components/LatestReceiptReprint.tsx, frontend/src/routes/pos.tsx, frontend/src/routes/__tests__/pos-receipt-replay.test.tsx</files>
  <behavior>
    - Test 1: POS route renders a lightweight latest-receipt affordance labeled exactly “Reprint receipt” when a latest receipt id exists per D-01 and D-08.
    - Test 2: offline state renders the same action disabled with concise explanatory copy per D-05.
    - Test 3: failure state displays non-destructive error copy without navigating away or clearing the cart per D-06.
  </behavior>
  <action>Create `LatestReceiptReprint` as a focused POS component that uses `useLatestReceipt()` and `useNetworkStatus()` through the hook contract from Task 1. Render nothing when there is no latest receipt id. When present, render a small inline/mobile-friendly action in the existing POS status area, with the button label exactly “Reprint receipt” per D-08. Use existing `Button` styles and Tailwind v4 utility classes; keep it lightweight, not a modal, sheet, table, or recent-orders list per D-02. Disable while offline or while replay is in progress, show “Receipt unavailable”/similar clear copy on failure, and never use “Reopen sale” wording per D-09. Wire this component into `frontend/src/routes/pos.tsx` near the existing item-count/cart-total pills so checkout success creates an immediately visible latest receipt affordance per D-01.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/routes/__tests__/pos-receipt-replay.test.tsx src/routes/__tests__/pos-shell.test.tsx</automated>
  </verify>
  <done>The POS selling floor shows a single lightweight “Reprint receipt” affordance for the latest receipt, disabled offline, with endpoint-backed replay behavior covered by tests and no recent-orders UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record the latest paid order during checkout finalization</name>
  <files>frontend/src/pos/components/CartPanel.tsx, frontend/src/routes/__tests__/pos-receipt-replay.test.tsx</files>
  <behavior>
    - Test 1: successful checkout stores the persisted backend order id from `created.data.id` after `api.completePayment()` succeeds per D-01 and D-07.
    - Test 2: fresh checkout printing still uses the current payment-completion `paymentResult.data` snapshot, while later reprint fetches persisted receipt through Task 1 per D-03 and D-07.
    - Test 3: failed checkout does not update latest receipt id and keeps the cashier in the existing POS state per D-06.
  </behavior>
  <action>Update `CartPanel` to import and call `useLatestReceipt()`. In `finalizeOrder`, after both `api.createOrder()` and `api.completePayment()` succeed, call `rememberLatestReceipt(created.data.id)` before clearing checkout state and before/around the existing `printReceipt(paymentResult.data)` call. Keep fresh checkout printing unchanged per D-07. Do not call `api.getReceipt()` during fresh checkout completion; only replay/reprint uses the persisted endpoint per D-03. Extend `pos-receipt-replay.test.tsx` with a CartPanel-focused test or mocked route integration proving `rememberLatestReceipt` is called only after successful payment and not on failure.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run src/routes/__tests__/pos-receipt-replay.test.tsx src/routes/__tests__/pos-shell.test.tsx</automated>
  </verify>
  <done>Completing a sale records the backend order id for later replay, immediate receipt printing still uses the payment snapshot, and persisted reprint remains endpoint-backed and tested.</done>
</task>

</tasks>

<verification>
Run targeted frontend tests from `frontend/`:

```bash
npm test -- --run src/routes/__tests__/pos-receipt-replay.test.tsx src/routes/__tests__/pos-shell.test.tsx
```

If the existing unrelated `frontend/src/routes/pos.tsx` unused `QuickKeysBar` import blocker from STATE.md appears in a full build, do not expand scope. Keep verification targeted to this receipt replay behavior.
</verification>

<success_criteria>
- `REC-03` replay gap is closed by a visible POS “Reprint receipt” action backed by `GET /api/orders/{id}/receipt`.
- Replay/reprint is disabled offline and failure shows a non-destructive error.
- Fresh checkout printing still works through the current payment-completion snapshot.
- No recent-orders list, sale editing/resume behavior, “Reopen sale” wording, digital receipt, customer history, or new payment capability is added.
- Targeted Vitest regression tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/08-receipt-replay-reopen-closure/08-01-SUMMARY.md`.
</output>
