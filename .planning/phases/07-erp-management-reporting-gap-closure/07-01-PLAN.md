---
phase: 07-erp-management-reporting-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/erp/products/ProductDrawer.tsx
  - frontend/src/erp/tables/ProductTable.tsx
  - frontend/src/erp/__tests__/erp-management.test.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-05
gap_closure: true
must_haves:
  truths:
    - "Owner can create and edit products with nested variants"
    - "Owner can archive products and individual variants"
    - "Owner can assign or generate barcodes without breaking the drawer contract"
  artifacts:
    - path: frontend/src/erp/products/ProductDrawer.tsx
      provides: "Product create/edit drawer contract"
    - path: frontend/src/erp/tables/ProductTable.tsx
      provides: "Product archive and variant reorder table"
    - path: frontend/src/erp/__tests__/erp-management.test.tsx
      provides: "Regression coverage for product flows"
  key_links:
    - from: frontend/src/erp/__tests__/erp-management.test.tsx
      to: frontend/src/erp/products/ProductDrawer.tsx
      via: "rendered field and variant assertions"
      pattern: "ProductDrawer|Variant 1|Save product"
    - from: frontend/src/erp/tables/ProductTable.tsx
      to: frontend/src/erp/products/ProductDrawer.tsx
      via: "edit/archive table actions"
      pattern: "onEditProduct|onArchiveProduct|onArchiveVariant"
---

<objective>
Close the product/variant verification gaps so the ERP catalog surface stays explicitly covered after the audit.

Purpose: Keep the core product drawer and product table behavior locked to the product-management requirements.
Output: Re-verified product drawer/table code plus focused Vitest coverage.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-erp-management-reporting-gap-closure/07-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/phases/04-erp-management-reporting/VERIFICATION.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
@.planning/phases/04-erp-management-reporting/04-08-SUMMARY.md
@.planning/phases/04-erp-management-reporting/04-09-SUMMARY.md

<interfaces>
From `frontend/src/erp/products/ProductDrawer.tsx`:
```ts
type ProductDrawerProps = {
  open: boolean
  product?: CatalogProductRecord | null
  categories: CatalogCategory[]
  onOpenChange: (open: boolean) => void
  onSave: (values: ProductFormValues) => void | Promise<void>
}
```

From `frontend/src/erp/tables/ProductTable.tsx`:
```ts
type ProductTableProps = {
  products: CatalogProductRecord[]
  categories: CatalogCategory[]
  archiveBusy?: boolean
  onCreateProduct: () => void
  onEditProduct: (product: CatalogProductRecord) => void
  onArchiveProduct: (product: CatalogProductRecord) => void
  onArchiveVariant: (product: CatalogProductRecord, variantId: string) => void
  onReorderVariants: (productId: string, variantIds: string[]) => void
}
```
</interfaces>

@frontend/src/erp/products/ProductDrawer.tsx
@frontend/src/erp/tables/ProductTable.tsx
@frontend/src/erp/__tests__/erp-management.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-assert product drawer contracts</name>
  <files>frontend/src/erp/products/ProductDrawer.tsx, frontend/src/erp/__tests__/erp-management.test.tsx</files>
  <behavior>
    - Product drawer still normalizes an existing record into editable fields.
    - Nested variants keep SKU, barcode, price, cost, and active toggles.
    - Image upload and THB preview stay wired through the same drawer contract.
  </behavior>
  <action>Strengthen the product drawer regression coverage first, then fix any contract drift in the drawer itself. Keep the nested-variant model and THB preview logic intact; do not flatten products or replace the existing drawer pattern.</action>
  <verify><automated>pnpm --dir frontend vitest run src/erp/__tests__/erp-management.test.tsx</automated></verify>
  <done>Product creation/edit behavior and variant editing remain covered by the focused regression suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-assert product table actions</name>
  <files>frontend/src/erp/tables/ProductTable.tsx, frontend/src/erp/__tests__/erp-management.test.tsx</files>
  <behavior>
    - Table keeps archive controls, reorder arrows, and variant archive actions.
    - Price ranges continue to format from live variant prices in THB.
    - Empty and populated states stay readable for owner review.
  </behavior>
  <action>Update the product table only if the regression suite shows a mismatch in action wiring or THB display. Preserve the existing archive/reorder contract and keep the variant row model unchanged.</action>
  <verify><automated>pnpm --dir frontend vitest run src/erp/__tests__/erp-management.test.tsx</automated></verify>
  <done>Archive, reorder, and price-range behavior are explicitly re-verified for the ERP product table.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the product suite passes end-to-end</name>
  <files>frontend/src/erp/products/ProductDrawer.tsx, frontend/src/erp/tables/ProductTable.tsx, frontend/src/erp/__tests__/erp-management.test.tsx</files>
  <action>Run the focused management spec after the assertions above. Fix only the gaps the test output proves, and keep the change surface constrained to the catalog UI contract.</action>
  <verify><automated>pnpm --dir frontend vitest run src/erp/__tests__/erp-management.test.tsx</automated></verify>
  <done>The product-management regression suite is green with the drawer/table contract intact.</done>
</task>

</tasks>

<verification>
Targeted Vitest for the ERP management suite passes without requiring unrelated ERP or backend tests.
</verification>

<success_criteria>
- Product creation, edit, archive, and nested-variant behavior are still verified.
- Variant barcode handling remains covered by the product-management regression suite.
- The plan can be executed without touching unrelated ERP reporting files.
</success_criteria>

<output>
After completion, create `.planning/phases/07-erp-management-reporting-gap-closure/07-01-SUMMARY.md`
</output>
