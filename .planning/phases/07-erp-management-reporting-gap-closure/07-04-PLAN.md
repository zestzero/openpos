---
phase: 07-erp-management-reporting-gap-closure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/erp/reports/ReportExportButtons.tsx
  - frontend/src/erp/reports/exportReport.ts
  - frontend/src/lib/formatCurrency.ts
  - frontend/src/erp/__tests__/report-export.test.tsx
autonomous: true
requirements:
  - RPT-03
  - PLAT-05
gap_closure: true
must_haves:
  truths:
    - "Owner can export the active reporting view to PDF and XLSX"
    - "Export filenames stay derived from the visible report range"
    - "All exported money values stay formatted as THB"
  artifacts:
    - path: frontend/src/erp/reports/ReportExportButtons.tsx
      provides: "Report export actions"
    - path: frontend/src/erp/reports/exportReport.ts
      provides: "PDF/XLSX export helpers and filename builder"
    - path: frontend/src/lib/formatCurrency.ts
      provides: "Canonical THB formatter"
    - path: frontend/src/erp/__tests__/report-export.test.tsx
      provides: "Regression coverage for export helper and THB formatting"
  key_links:
    - from: frontend/src/erp/reports/ReportExportButtons.tsx
      to: frontend/src/erp/reports/exportReport.ts
      via: "button click helper invocation"
      pattern: "exportReportRows('pdf')|exportReportRows('xlsx')"
    - from: frontend/src/erp/reports/exportReport.ts
      to: frontend/src/lib/formatCurrency.ts
      via: "shared THB formatting for PDF and XLSX payloads"
      pattern: "formatTHB|buildReportExportFilename"
---

<objective>
Close the report-export and THB-formatting verification gaps so report downloads and currency display stay explicitly covered.

Purpose: Keep PDF/XLSX export behavior and THB formatting tied to the audited ERP requirements.
Output: Re-verified export helper code plus focused Vitest coverage.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-erp-management-reporting-gap-closure/07-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/phases/04-erp-management-reporting/VERIFICATION.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
@.planning/phases/04-erp-management-reporting/04-08-SUMMARY.md
@.planning/phases/04-erp-management-reporting/04-09-SUMMARY.md

<interfaces>
From `frontend/src/erp/reports/ReportExportButtons.tsx`:
```ts
type ReportExportButtonsProps = {
  title: string
  rows: ReportingMonthRow[]
}
```

From `frontend/src/erp/reports/exportReport.ts`:
```ts
export type ReportExportFormat = 'pdf' | 'xlsx'
export type ReportExportPayload = {
  title: string
  rows: ReportingMonthRow[]
}
export function buildReportExportFilename(format: ReportExportFormat, title: string, rows: Pick<ReportingMonthRow, 'month'>[]): string
export function exportReportRows(format: ReportExportFormat, payload: ReportExportPayload): void
```

From `frontend/src/lib/formatCurrency.ts`:
```ts
export function formatCurrency(amount: number): string
export function formatTHB(amount: number): string
```
</interfaces>

@frontend/src/erp/reports/ReportExportButtons.tsx
@frontend/src/erp/reports/exportReport.ts
@frontend/src/lib/formatCurrency.ts
@frontend/src/erp/__tests__/report-export.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused export helper coverage</name>
  <files>frontend/src/erp/reports/exportReport.ts, frontend/src/erp/__tests__/report-export.test.tsx</files>
  <behavior>
    - Filename derivation still respects single-month and ranged-month exports.
    - PDF and XLSX payloads keep the same rows and THB-formatted money fields.
    - The export helper stays tied to the visible report range instead of fetching again.
  </behavior>
  <action>Create a dedicated export regression spec and keep the helper implementation aligned with it. Preserve the jsPDF and SheetJS pipeline; do not replace the export libraries.</action>
  <verify><automated>pnpm --dir frontend vitest run src/erp/__tests__/report-export.test.tsx</automated></verify>
  <done>Export filename and payload behavior are explicitly covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-assert THB formatting and export button wiring</name>
  <files>frontend/src/erp/reports/ReportExportButtons.tsx, frontend/src/lib/formatCurrency.ts, frontend/src/erp/__tests__/report-export.test.tsx</files>
  <behavior>
    - Report export buttons still call the helper for PDF and XLSX.
    - `formatTHB` remains the single export/display formatter.
    - Exported money strings stay in Thai Baht, not ad hoc currency text.
  </behavior>
  <action>Update the export buttons or shared formatter only if the new regression shows a mismatch. Keep the UI button contract intact and preserve the satang-to-THB formatting rule.</action>
  <verify><automated>pnpm --dir frontend vitest run src/erp/__tests__/report-export.test.tsx</automated></verify>
  <done>The export and THB-formatting regression suite is green.</done>
</task>

</tasks>

<verification>
Focused Vitest for the report export suite passes, proving filename derivation, helper wiring, and THB formatting remain intact.
</verification>

<success_criteria>
- PDF and XLSX exports stay tied to the current dashboard rows.
- Export filenames remain range-aware.
- THB formatting remains centralized in the shared currency helper.
</success_criteria>

<output>
After completion, create `.planning/phases/07-erp-management-reporting-gap-closure/07-04-SUMMARY.md`
</output>
