---
phase: 07-erp-management-reporting-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/erp/categories/CategoryDrawer.tsx
  - frontend/src/erp/tables/CategoryTable.tsx
  - frontend/src/erp/import/ImportDrawer.tsx
  - frontend/src/erp/__tests__/erp-import.test.tsx
autonomous: true
requirements:
  - PROD-04
  - PROD-06
gap_closure: true
must_haves:
  truths:
    - "Owner can create, edit, and reorder categories"
    - "Owner can import products and variants from CSV or XLSX"
    - "Invalid import rows are blocked before submit"
  artifacts:
    - path: frontend/src/erp/categories/CategoryDrawer.tsx
      provides: "Category create/edit drawer contract"
    - path: frontend/src/erp/tables/CategoryTable.tsx
      provides: "Category reorder table contract"
    - path: frontend/src/erp/import/ImportDrawer.tsx
      provides: "Spreadsheet import preview and submit flow"
    - path: frontend/src/erp/__tests__/erp-import.test.tsx
      provides: "Regression coverage for import and barcode generation"
  key_links:
    - from: frontend/src/erp/__tests__/erp-import.test.tsx
      to: frontend/src/erp/import/ImportDrawer.tsx
      via: "preview parsing and submit payload assertions"
      pattern: "Import CSV|Import validated rows|/api/catalog/import"
    - from: frontend/src/erp/tables/CategoryTable.tsx
      to: frontend/src/erp/categories/CategoryDrawer.tsx
      via: "create/edit/reorder controls"
      pattern: "onCreateCategory|onEditCategory|onReorderCategories"
---

<objective>
Close the category and catalog-import verification gaps so the ERP’s organization and bulk-import flows are explicitly re-verified.

Purpose: Keep category ordering and spreadsheet imports aligned with the audited ERP requirements.
Output: Re-verified category/import code plus focused Vitest coverage.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-erp-management-reporting-gap-closure/07-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/phases/04-erp-management-reporting/VERIFICATION.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
@.planning/phases/04-erp-management-reporting/04-08-SUMMARY.md
@.planning/phases/04-erp-management-reporting/04-09-SUMMARY.md

<interfaces>
From `frontend/src/erp/categories/CategoryDrawer.tsx`:
```ts
type CategoryDrawerProps = {
  open: boolean
  category?: CatalogCategory | null
  categories: CatalogCategory[]
  onOpenChange: (open: boolean) => void
  onSave: (values: CategoryFormValues) => void | Promise<void>
}
```

From `frontend/src/erp/tables/CategoryTable.tsx`:
```ts
type CategoryTableProps = {
  categories: CatalogCategory[]
  reorderBusy?: boolean
  onCreateCategory: () => void
  onEditCategory: (category: CatalogCategory) => void
  onReorderCategories: (ids: string[]) => void
}
```
</interfaces>

@frontend/src/erp/categories/CategoryDrawer.tsx
@frontend/src/erp/tables/CategoryTable.tsx
@frontend/src/erp/import/ImportDrawer.tsx
@frontend/src/erp/__tests__/erp-import.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-assert category drawer and table wiring</name>
  <files>frontend/src/erp/categories/CategoryDrawer.tsx, frontend/src/erp/tables/CategoryTable.tsx, frontend/src/erp/__tests__/erp-import.test.tsx</files>
  <behavior>
    - Category drawer still exposes parent-category and description controls.
    - Category table still supports create, edit, and reorder interactions.
    - Empty-state and row-order behavior stay predictable for the owner.
  </behavior>
  <action>Strengthen category verification first, then fix any drift in the drawer/table props or reorder handling. Keep the category surface table-first and do not add new category concepts.</action>
  <verify><automated>pnpm --dir frontend vitest run src/erp/__tests__/erp-import.test.tsx</automated></verify>
  <done>Category create/edit/reorder behavior is explicitly re-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-assert spreadsheet import parsing and validation</name>
  <files>frontend/src/erp/import/ImportDrawer.tsx, frontend/src/erp/__tests__/erp-import.test.tsx</files>
  <behavior>
    - CSV/XLSX preview parsing still recognizes the documented header aliases.
    - Invalid rows stay blocked before submit and duplicate SKU/barcode detection remains intact.
    - Barcode generation and POST payload shape stay aligned with the import contract.
  </behavior>
  <action>Update the import drawer only if the regression suite proves a parsing, validation, or payload-shape mismatch. Keep the preview-first workflow and avoid replacing the SheetJS pipeline.</action>
  <verify><automated>pnpm --dir frontend vitest run src/erp/__tests__/erp-import.test.tsx</automated></verify>
  <done>CSV/XLSX import preview, validation, and submit behavior remain covered by the focused regression suite.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the import suite passes end-to-end</name>
  <files>frontend/src/erp/categories/CategoryDrawer.tsx, frontend/src/erp/tables/CategoryTable.tsx, frontend/src/erp/import/ImportDrawer.tsx, frontend/src/erp/__tests__/erp-import.test.tsx</files>
  <action>Run the focused import spec after the assertions above. Fix only the proven gap, and keep changes limited to the category/import contract.</action>
  <verify><automated>pnpm --dir frontend vitest run src/erp/__tests__/erp-import.test.tsx</automated></verify>
  <done>The category and import regression suite is green.</done>
</task>

</tasks>

<verification>
Targeted Vitest for the category/import suite passes without requiring unrelated reporting tests.
</verification>

<success_criteria>
- Category create/edit/reorder behavior is still verified.
- CSV/XLSX import validation blocks bad rows before submit.
- Barcode generation stays covered by the import regression suite.
</success_criteria>

<output>
After completion, create `.planning/phases/07-erp-management-reporting-gap-closure/07-02-SUMMARY.md`
</output>
