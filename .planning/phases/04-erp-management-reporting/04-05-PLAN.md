---
phase: 04-erp-management-reporting
plan: 05
type: execute
wave: 2
depends_on: ["04-04"]
files_modified:
  - frontend/src/routeTree.gen.ts
  - frontend/src/routes/erp.tsx
  - frontend/src/routes/erp.index.tsx
  - frontend/src/erp/layout/ErpLayout.tsx
  - frontend/src/erp/navigation/ErpNav.tsx
  - frontend/src/erp/__tests__/erp-shell.test.tsx
autonomous: true
requirements: [PROD-01, PROD-03, PROD-04]
must_haves:
  truths:
    - "Owners land on a real ERP shell instead of a placeholder card"
    - "The ERP route remains owner-gated"
    - "The desktop shell exposes the management outlet and tabbed structure"
  artifacts:
    - path: "frontend/src/routes/erp.tsx"
      provides: "Owner-gated ERP route"
    - path: "frontend/src/erp/layout/ErpLayout.tsx"
      provides: "Desktop ERP shell"
    - path: "frontend/src/erp/navigation/ErpNav.tsx"
      provides: "Persistent left navigation"
  key_links:
    - from: "frontend/src/routes/erp.tsx"
      to: "frontend/src/erp/layout/ErpLayout.tsx"
      via: "outlet-based shell composition"
      pattern: "getStoredSession|<Outlet"
---

<objective>
Replace the ERP placeholder with the desktop shell.

Purpose: establish the locked left-nav / top-bar / tabbed composition before management workflows arrive.
Output: owner-gated ERP route, layout, and shell tests.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn the ERP route into an owner-gated shell</name>
  <files>frontend/src/routeTree.gen.ts, frontend/src/routes/erp.tsx, frontend/src/routes/erp.index.tsx</files>
  <read_first>
    Read `frontend/src/routes/erp.tsx`, `frontend/src/routes/__root.tsx`, `frontend/src/lib/auth.ts`, `.planning/ROADMAP.md`, and `.planning/phases/04-erp-management-reporting/04-UI-SPEC.md` before editing.
  </read_first>
  <action>
    Keep the owner guard, replace the placeholder card with an outlet-based route tree, and add the ERP index route for the management landing page.
  </action>
  <acceptance_criteria>
    - `grep -n "getStoredSession\|<Outlet\|erp.index" frontend/src/routes/erp.tsx frontend/src/routes/erp.index.tsx` finds the shell route wiring.
    - `cd frontend && npm exec vitest run src/erp/__tests__/erp-shell.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/erp-shell.test.tsx</automated>
  </verify>
  <done>The ERP route renders a real shell and still protects owners-only access.</done>
</task>

<task type="auto">
  <name>Task 2: Build the desktop layout and navigation shell</name>
  <files>frontend/src/erp/layout/ErpLayout.tsx, frontend/src/erp/navigation/ErpNav.tsx, frontend/src/erp/__tests__/erp-shell.test.tsx</files>
  <read_first>
    Read `frontend/src/erp/layout/ErpLayout.tsx`, `frontend/src/erp/navigation/ErpNav.tsx`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/phases/04-erp-management-reporting/04-UI-SPEC.md` before editing.
  </read_first>
  <action>
    Compose the left navigation, top utility bar, and reporting tab trigger so the shell matches the locked desktop ERP layout.
  </action>
  <acceptance_criteria>
    - `grep -n "left\|navigation\|tabs\|top" frontend/src/erp/layout/ErpLayout.tsx frontend/src/erp/navigation/ErpNav.tsx` finds the desktop shell pieces.
    - `cd frontend && npm exec vitest run src/erp/__tests__/erp-shell.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/erp-shell.test.tsx</automated>
  </verify>
  <done>The ERP shell matches the locked layout contract and is ready for management views.</done>
</task>

</tasks>

<verification>
Frontend verification: the ERP shell exists, is owner-gated, and renders the locked desktop composition.
</verification>

<success_criteria>
- ERP route stays owner-gated.
- Desktop shell shows left nav, top bar, and tabs.
- Shell tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-05-SUMMARY.md`
</output>
