---
phase: 04-erp-management-reporting
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - db/migrations/009_add_reporting_read_models.up.sql
  - db/migrations/009_add_reporting_read_models.down.sql
  - db/queries/reporting.sql
  - db/sqlc/reporting.sql.go
  - internal/reporting/service.go
  - internal/reporting/handler.go
  - internal/reporting/service_test.go
  - cmd/server/main.go
autonomous: true
requirements: [RPT-01, RPT-02]
must_haves:
  truths:
    - "The backend can return monthly sales totals, order counts, and average order value"
    - "The backend can calculate gross profit from a sale-time cost basis"
    - "The reporting API is mounted under /api/reports"
  artifacts:
    - path: "db/queries/reporting.sql"
      provides: "Monthly sales and gross profit queries"
    - path: "internal/reporting/service.go"
      provides: "Reporting read model service"
    - path: "internal/reporting/handler.go"
      provides: "Reporting HTTP routes"
  key_links:
    - from: "internal/reporting/service.go"
      to: "db/queries/reporting.sql"
      via: "monthly sales and gross profit aggregations"
      pattern: "monthly-sales|gross-profit|cost_at_sale"
    - from: "cmd/server/main.go"
      to: "internal/reporting/handler.go"
      via: "route mount under /api/reports"
      pattern: "Mount\(\"/reports\""
---

<objective>
Expose owner reporting APIs for monthly sales and gross profit.

Purpose: give the ERP UI stable read models to render summaries and profit.
Output: reporting SQL, service, handler, and server route wiring.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement reporting read models</name>
  <files>db/migrations/009_add_reporting_read_models.up.sql, db/migrations/009_add_reporting_read_models.down.sql, db/queries/reporting.sql, db/sqlc/reporting.sql.go, internal/reporting/service.go</files>
  <read_first>
    Read `db/queries/reporting.sql`, `internal/sales/service.go`, `internal/sales/service_test.go`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/phases/04-erp-management-reporting/04-RESEARCH.md` before editing.
  </read_first>
  <action>
    Add sqlc queries and a reporting service that return monthly sales totals and gross profit using the sale-time cost snapshot.
  </action>
  <acceptance_criteria>
    - `grep -n "monthly-sales\|gross-profit\|cost_at_sale" db/queries/reporting.sql internal/reporting/service.go` finds the two reporting read models.
    - `go test ./internal/reporting ./...` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/reporting ./...</automated>
  </verify>
  <done>Reporting data can be queried from the backend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount and verify the reporting routes</name>
  <files>internal/reporting/handler.go, internal/reporting/service_test.go, cmd/server/main.go</files>
  <read_first>
    Read `internal/reporting/handler.go`, `cmd/server/main.go`, `.planning/ROADMAP.md`, and `.planning/phases/04-erp-management-reporting/04-CONTEXT.md` before wiring the routes.
  </read_first>
  <action>
    Expose `GET /api/reports/monthly-sales` and `GET /api/reports/gross-profit`, mount the reporting handler in the main server, and add tests that cover the response shape.
  </action>
  <acceptance_criteria>
    - `grep -n "/api/reports\|monthly-sales\|gross-profit" internal/reporting/handler.go cmd/server/main.go` finds the mounted route paths.
    - `go test ./internal/reporting ./...` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/reporting ./...</automated>
  </verify>
  <done>The backend exposes the reporting API under /api/reports.</done>
</task>

</tasks>

<verification>
Backend verification: monthly sales and gross profit endpoints exist and are mounted under /api/reports.
</verification>

<success_criteria>
- Monthly sales and gross profit read models return data from the backend.
- Reporting routes are mounted at `/api/reports`.
- Reporting tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-02-SUMMARY.md`
</output>
