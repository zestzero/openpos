---
phase: 04-erp-management-reporting
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/routeTree.gen.ts
  - frontend/src/routes/erp.tsx
  - frontend/src/routes/erp.index.tsx
  - frontend/src/erp/layout/ErpLayout.tsx
  - frontend/src/erp/navigation/ErpNav.tsx
  - frontend/src/erp/tables/ProductTable.tsx
  - frontend/src/erp/tables/CategoryTable.tsx
  - frontend/src/erp/__tests__/vitest-setup.test.ts
  - frontend/src/erp/__tests__/erp-shell.test.tsx
  - frontend/src/lib/erp-api.ts
  - frontend/src/erp/products/ProductDrawer.tsx
  - frontend/src/erp/categories/CategoryDrawer.tsx
  - frontend/src/erp/import/ImportDrawer.tsx
  - frontend/src/erp/products/variantBarcode.ts
  - frontend/src/erp/__tests__/erp-management.test.tsx
autonomous: true
requirements: [PROD-01, PROD-02, PROD-03, PROD-04, PROD-05, PROD-06, PLAT-05]
must_haves:
  truths:
    - "Owner lands on a desktop ERP shell with persistent left navigation, a top utility bar, and tabs"
    - "Owner can create, edit, archive, and reorder products, variants, and categories from the table-first management view"
    - "Owner can bulk import products and variants from CSV or XLSX and see validation errors before submit"
    - "All ERP money fields display in THB using the shared formatter"
  artifacts:
    - path: "frontend/src/routes/erp.tsx"
      provides: "Owner-gated ERP shell route"
    - path: "frontend/src/erp/layout/ErpLayout.tsx"
      provides: "Desktop shell composition"
    - path: "frontend/src/lib/erp-api.ts"
      provides: "ERP catalog API hooks"
    - path: "frontend/vitest.config.ts"
      provides: "ERP UI test runner"
  key_links:
    - from: "frontend/src/routes/erp.tsx"
      to: "frontend/src/erp/layout/ErpLayout.tsx"
      via: "owner guard + outlet shell"
      pattern: "getStoredSession|<Outlet"
    - from: "frontend/src/erp/products/ProductDrawer.tsx"
      to: "frontend/src/lib/erp-api.ts"
      via: "TanStack Query mutations for create/update/archive"
      pattern: "useMutation|api\."
    - from: "frontend/src/erp/import/ImportDrawer.tsx"
      to: "frontend/src/erp/products/variantBarcode.ts"
      via: "parsed rows and generated barcodes"
      pattern: "XLSX|barcode"
---

<objective>
Build the owner-facing ERP shell and management workspace for products, variants, categories, and imports.

Purpose: give owners a desktop-first management surface that matches the locked UI contract before reporting is wired in.
Output: ERP route scaffolding, Vitest setup, product/category tables and drawers, and import validation.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-UI-SPEC.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md

<interfaces>
From `frontend/src/lib/auth.ts`:
```ts
export function getStoredSession(): StoredSession | null
export function getRedirectPath(role: UserRole): string
export function persistSession(session: StoredSession): void
```

From `frontend/src/lib/api.ts`:
```ts
export interface Category { id: string; name: string; description: string | null; parent_id: string | null }
export interface Product { id: string; name: string; description: string | null; category_id: string | null; image_url: string | null; is_active: boolean }
export interface Variant { id: string; product_id: string; sku: string; barcode: string | null; name: string; price: number; cost: number | null; is_active: boolean }
export function requestJSON<T>(path: string, init?: RequestInit): Promise<T>
```

From `frontend/src/lib/formatCurrency.ts`:
```ts
export function formatTHB(amount: number): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Vitest and ERP UI test scaffolding</name>
  <files>frontend/package.json, frontend/vitest.config.ts, frontend/src/test/setup.ts, frontend/src/erp/__tests__/vitest-setup.test.ts</files>
  <action>
    Add the frontend test runner, jsdom test environment, and shared RTL setup so ERP UI work can be tested as it lands.

    Install the frontend libraries required by the phase (`vitest`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`, `@testing-library/user-event`, `recharts`, `xlsx`, `jspdf`, `jspdf-autotable`) and add a smoke test that proves the harness is wired.
  </action>
  <acceptance_criteria>
    - `frontend/package.json` contains a `vitest` test script and the phase dependencies.
    - `frontend/vitest.config.ts` sets `environment: 'jsdom'` and the `@` alias.
    - `frontend/src/test/setup.ts` imports `@testing-library/jest-dom`.
    - `frontend/src/erp/__tests__/vitest-setup.test.ts` exists and runs under Vitest.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/vitest-setup.test.ts</automated>
  </verify>
  <done>The frontend test harness runs in jsdom and can execute ERP-specific smoke tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the ERP placeholder with the desktop shell</name>
  <files>frontend/src/routeTree.gen.ts, frontend/src/routes/erp.tsx, frontend/src/routes/erp.index.tsx, frontend/src/erp/layout/ErpLayout.tsx, frontend/src/erp/navigation/ErpNav.tsx, frontend/src/erp/tables/ProductTable.tsx, frontend/src/erp/tables/CategoryTable.tsx, frontend/src/erp/__tests__/erp-shell.test.tsx</files>
  <behavior>
    - `/erp` stays owner-gated and renders a desktop-first shell, not a full-screen placeholder card.
    - The shell exposes persistent left navigation, a top utility bar, and tabs that separate management from reporting.
    - The management landing page is table-first and shows empty states instead of crashing when no data is returned.
  </behavior>
  <action>
    Turn `frontend/src/routes/erp.tsx` into the parent shell route, add the management index route, and build the ERP layout with the locked desktop composition from D-01 through D-04.

    Keep the owner guard intact, route the content through an outlet, and wire the shell around table-first management content that can later host the product/category drawers.
  </action>
  <acceptance_criteria>
    - `frontend/src/routes/erp.tsx` still checks `getStoredSession()` and renders an outlet instead of the placeholder card.
    - `frontend/src/routes/erp.index.tsx` exports the management landing page.
    - `frontend/src/erp/layout/ErpLayout.tsx` contains left nav, top bar, tabs, and a table-first workspace.
    - `frontend/src/erp/tables/ProductTable.tsx` and `frontend/src/erp/tables/CategoryTable.tsx` render empty states from the UI spec instead of hardcoded demo data.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/erp-shell.test.tsx</automated>
  </verify>
  <done>The ERP route resolves to a real owner shell with the locked desktop layout and a test proves the shell structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add product, category, and import workflows</name>
  <files>frontend/src/lib/erp-api.ts, frontend/src/erp/products/ProductDrawer.tsx, frontend/src/erp/categories/CategoryDrawer.tsx, frontend/src/erp/import/ImportDrawer.tsx, frontend/src/erp/products/variantBarcode.ts, frontend/src/erp/__tests__/erp-management.test.tsx</files>
  <behavior>
    - Product drawers support create/edit/archive flows with nested variants and per-variant SKU, barcode, price, and cost fields.
    - Category editing supports create, edit, and reorder interactions from the ERP surface.
    - Importing from CSV/XLSX validates rows first, surfaces errors before submit, and uses the same API helpers as the manual drawers.
    - Barcode generation fills the variant barcode field before submit when the owner chooses the generate action.
  </behavior>
  <action>
    Implement the CRUD drawers and query hooks against the existing catalog API, keeping the Product → Variant hierarchy intact and using `formatTHB` for every displayed money value.

    Parse spreadsheet uploads with `xlsx`, validate rows with Zod, show a preview/error summary, and submit batch creates only after the preview passes validation.
  </action>
  <acceptance_criteria>
    - `frontend/src/lib/erp-api.ts` contains product, variant, category, and reorder mutation helpers.
    - `frontend/src/erp/products/ProductDrawer.tsx` and `frontend/src/erp/categories/CategoryDrawer.tsx` use form validation instead of ad hoc state.
    - `frontend/src/erp/import/ImportDrawer.tsx` references `xlsx` and renders a validation summary before submit.
    - `frontend/src/erp/products/variantBarcode.ts` exposes a barcode helper used by the drawers.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/erp-management.test.tsx</automated>
  </verify>
  <done>Owners can manage catalog data and import spreadsheets from the ERP UI without leaving the desktop workspace.</done>
</task>

</tasks>

<verification>
Frontend verification: the ERP shell exists, management workflows are tested in Vitest, and the UI can load product/category/import flows from the owner route.
</verification>

<success_criteria>
- Owners see the desktop ERP shell with left nav, top bar, and management tabs.
- Product, variant, category, and import flows work through the ERP frontend and use THB formatting.
- The frontend test harness runs Vitest specs for ERP shell and management behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-02-SUMMARY.md`
</output>
