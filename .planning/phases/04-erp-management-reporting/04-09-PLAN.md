---
phase: 04-erp-management-reporting
plan: 09
type: execute
wave: 4
depends_on: ["04-08", "04-04"]
files_modified:
  - frontend/src/erp/reports/ReportExportButtons.tsx
  - frontend/src/erp/reports/exportReport.ts
  - frontend/src/erp/__tests__/reporting.test.tsx
autonomous: true
requirements: [RPT-03, PLAT-05]
must_haves:
  truths:
    - "Owners can export the current report to PDF or XLSX"
    - "Exports use the active report data, not a separate query"
  artifacts:
    - path: "frontend/src/erp/reports/ReportExportButtons.tsx"
      provides: "Export controls"
    - path: "frontend/src/erp/reports/exportReport.ts"
      provides: "PDF/XLSX generation helpers"
  key_links:
    - from: "frontend/src/erp/reports/ReportExportButtons.tsx"
      to: "frontend/src/erp/reports/exportReport.ts"
      via: "download buttons"
      pattern: "jspdf|xlsx"
---

<objective>
Add PDF and XLSX exports for the active report.

Purpose: let owners download the same report they are viewing.
Output: export buttons, generators, and test coverage.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
@.planning/phases/04-erp-management-reporting/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate PDF and XLSX exports from the report rows</name>
  <files>frontend/src/erp/reports/ReportExportButtons.tsx, frontend/src/erp/reports/exportReport.ts</files>
  <read_first>
    Read `frontend/src/erp/reports/ReportDashboard.tsx`, `frontend/src/erp/reports/ReportCards.tsx`, `.planning/ROADMAP.md`, and `.planning/phases/04-erp-management-reporting/04-RESEARCH.md` before editing.
  </read_first>
  <action>
    Turn the active report rows into PDF via `jspdf` + `jspdf-autotable` and XLSX via `xlsx`, keeping the report filter state unchanged.
  </action>
  <acceptance_criteria>
    - `grep -n "jspdf\|autotable\|xlsx" frontend/src/erp/reports/ReportExportButtons.tsx frontend/src/erp/reports/exportReport.ts` finds both export libraries.
    - `cd frontend && npm exec vitest run src/erp/__tests__/reporting.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/reporting.test.tsx</automated>
  </verify>
  <done>The active report can be downloaded as PDF or XLSX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify export behavior in the reporting tests</name>
  <files>frontend/src/erp/__tests__/reporting.test.tsx</files>
  <read_first>
    Read `frontend/src/erp/__tests__/reporting.test.tsx`, `.planning/ROADMAP.md`, and `.planning/phases/04-erp-management-reporting/04-UI-SPEC.md` before editing.
  </read_first>
  <action>
    Add tests that prove the export buttons use the active dashboard data and emit the expected filenames.
  </action>
  <acceptance_criteria>
    - `grep -n "export\|pdf\|xlsx" frontend/src/erp/__tests__/reporting.test.tsx` finds export coverage.
    - `cd frontend && npm exec vitest run src/erp/__tests__/reporting.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/reporting.test.tsx</automated>
  </verify>
  <done>Export behavior is covered by tests.</done>
</task>

</tasks>

<verification>
Frontend verification: report exports produce PDF and XLSX from the active view.
</verification>

<success_criteria>
- Report exports work in PDF and XLSX.
- Exports use the active data.
- Export tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-09-SUMMARY.md`
</output>
