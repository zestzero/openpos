---
phase: 04-erp-management-reporting
plan: 07
type: execute
wave: 3
depends_on: ["04-04", "04-05"]
files_modified:
  - frontend/src/lib/erp-api.ts
  - frontend/src/erp/import/ImportDrawer.tsx
  - frontend/src/erp/products/variantBarcode.ts
  - internal/catalog/service.go
  - internal/catalog/handler.go
  - cmd/server/main.go
  - frontend/src/erp/__tests__/erp-import.test.tsx
autonomous: true
requirements: [PROD-05, PROD-06, PLAT-05]
must_haves:
  truths:
    - "Owners can generate or assign barcodes for variants"
    - "Owners can preview CSV/XLSX imports before submit"
    - "Validated import rows are submitted to the catalog backend and persisted"
    - "Import validation shows row-level errors"
  artifacts:
    - path: "frontend/src/erp/import/ImportDrawer.tsx"
      provides: "Bulk import UI with submit action"
    - path: "frontend/src/erp/products/variantBarcode.ts"
      provides: "Variant barcode helper"
  key_links:
    - from: "frontend/src/erp/import/ImportDrawer.tsx"
      to: "frontend/src/lib/erp-api.ts"
      via: "validated import submit mutation"
      pattern: "importProducts|useMutation|POST /api/catalog/import"
    - from: "frontend/src/lib/erp-api.ts"
      to: "internal/catalog/handler.go"
      via: "POST /api/catalog/import endpoint"
      pattern: "catalog/import|CreateImport"
    - from: "frontend/src/erp/import/ImportDrawer.tsx"
      to: "frontend/src/erp/products/variantBarcode.ts"
      via: "generate barcode action"
      pattern: "barcode"
---

<objective>
Add barcode and spreadsheet import workflows.

Purpose: let owners assign barcodes and bulk import products/variants from files.
Output: barcode helper, import drawer, and import tests.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
@.planning/phases/04-erp-management-reporting/04-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement barcode helpers and import parsing</name>
  <files>frontend/src/erp/import/ImportDrawer.tsx, frontend/src/erp/products/variantBarcode.ts</files>
  <read_first>
    Read `frontend/src/erp/import/ImportDrawer.tsx`, `frontend/src/erp/products/variantBarcode.ts`, `.planning/ROADMAP.md`, and `.planning/phases/04-erp-management-reporting/04-RESEARCH.md` before editing.
  </read_first>
  <action>
    Parse CSV/XLSX uploads, generate variant barcodes on demand, stage a preview of imported rows before submit, and submit validated rows through `frontend/src/lib/erp-api.ts` to a `POST /api/catalog/import` endpoint.

    The backend import endpoint must create products and nested variants from the validated payload so the import flow completes instead of stopping at preview.
  </action>
  <acceptance_criteria>
    - `grep -n "xlsx\|barcode\|preview\|importProducts\|POST /api/catalog/import" frontend/src/lib/erp-api.ts frontend/src/erp/import/ImportDrawer.tsx frontend/src/erp/products/variantBarcode.ts internal/catalog/service.go internal/catalog/handler.go` finds the submit wiring.
    - The drawer renders validation errors before submit.
    - The import flow has a submit action that persists validated rows, not just a preview.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/erp-import.test.tsx</automated>
  </verify>
  <done>Owners can generate barcodes and preview imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add import validation coverage</name>
  <files>frontend/src/erp/__tests__/erp-import.test.tsx</files>
  <read_first>
    Read `frontend/src/erp/__tests__/erp-import.test.tsx`, `.planning/ROADMAP.md`, and `.planning/phases/04-erp-management-reporting/04-UI-SPEC.md` before editing.
  </read_first>
  <action>
    Add tests that prove bad rows are blocked, preview errors are shown, and valid imports can be staged for submission.
  </action>
  <acceptance_criteria>
    - `grep -n "preview\|error\|barcode" frontend/src/erp/__tests__/erp-import.test.tsx` finds import validation coverage.
    - `cd frontend && npm exec vitest run src/erp/__tests__/erp-import.test.tsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/erp-import.test.tsx</automated>
  </verify>
  <done>Import validation and barcode generation are test-covered.</done>
</task>

</tasks>

<verification>
Frontend verification: barcode generation and spreadsheet import validation work from the ERP UI.
</verification>

<success_criteria>
- Variant barcodes can be generated or entered.
- CSV/XLSX imports show row-level errors before submit.
- Import tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-07-SUMMARY.md`
</output>
