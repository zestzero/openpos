---
phase: 04-erp-management-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/008_add_order_item_cost_snapshot.up.sql
  - db/migrations/008_add_order_item_cost_snapshot.down.sql
  - db/queries/sales.sql
  - db/sqlc/sales.sql.go
  - internal/sales/service.go
  - internal/sales/service_test.go
  - db/migrations/009_add_reporting_read_models.up.sql
  - db/migrations/009_add_reporting_read_models.down.sql
  - db/queries/reporting.sql
  - db/sqlc/reporting.sql.go
  - internal/reporting/service.go
  - internal/reporting/handler.go
  - internal/reporting/service_test.go
  - cmd/server/main.go
  - db/migrations/010_add_category_sort_order.up.sql
  - db/migrations/010_add_category_sort_order.down.sql
  - db/queries/catalog.sql
  - db/sqlc/catalog.sql.go
  - internal/catalog/service.go
  - internal/catalog/handler.go
autonomous: true
requirements: [RPT-01, RPT-02, PROD-04]
must_haves:
  truths:
    - "Gross profit uses a sale-time cost basis, not the current editable variant cost"
    - "Owner can view monthly sales summary and gross profit data from the backend"
    - "Category ordering is persisted so the ERP UI can reorder categories predictably"
  artifacts:
    - path: "db/migrations/008_add_order_item_cost_snapshot.up.sql"
      provides: "Sale-time cost snapshot column"
    - path: "db/queries/reporting.sql"
      provides: "Monthly sales and gross profit read queries"
    - path: "internal/reporting/service.go"
      provides: "Reporting read models for ERP"
    - path: "db/queries/catalog.sql"
      provides: "Category sort/reorder support"
  key_links:
    - from: "internal/sales/service.go"
      to: "db/queries/sales.sql"
      via: "CreateOrderItem inserts cost_at_sale from the live variant"
      pattern: "cost_at_sale"
    - from: "internal/reporting/service.go"
      to: "db/queries/reporting.sql"
      via: "monthly sales and gross profit aggregation"
      pattern: "COALESCE\(order_items\.cost_at_sale, variants\.cost\)"
    - from: "internal/catalog/handler.go"
      to: "db/queries/catalog.sql"
      via: "reorder endpoint updates category sort_order"
      pattern: "sort_order"
---

<objective>
Add the backend reporting and category-order foundations the ERP UI depends on.

Purpose: make gross profit trustworthy, expose report data, and persist category ordering before the frontend tabs ship.
Output: cost-snapshot migration, reporting package/routes, and category reorder support.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md

<interfaces>
From `internal/sales/service.go`:
```go
type orderStore interface {
    CreateOrder(context.Context, sqlc.CreateOrderParams) (sqlc.Order, error)
    CreateOrderItem(context.Context, sqlc.CreateOrderItemParams) (sqlc.OrderItem, error)
    GetVariant(context.Context, pgtype.UUID) (sqlc.Variant, error)
}

type CompletePaymentInput struct {
    OrderID        string
    Method         string
    TenderedAmount int64
    StoreName      string
}
```

From `internal/catalog/service.go`:
```go
type CreateCategoryInput struct {
    Name        string
    Description string
    ParentID    *string
}

func (s *Service) ListCategories(ctx context.Context) ([]sqlc.Category, error)
func (s *Service) UpdateCategory(ctx context.Context, id string, input CreateCategoryInput) (sqlc.Category, error)
```

From `cmd/server/main.go`:
```go
r.Mount("/api", protected)
protected.Mount("/catalog", catalogHandler.Routes())
protected.Mount("/orders", salesHandler.Routes())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Snapshot variant cost when an order is created</name>
  <files>db/migrations/008_add_order_item_cost_snapshot.up.sql, db/migrations/008_add_order_item_cost_snapshot.down.sql, db/queries/sales.sql, db/sqlc/sales.sql.go, internal/sales/service.go, internal/sales/service_test.go</files>
  <behavior>
    - Completed orders write the current variant cost into `order_items.cost_at_sale` at insert time.
    - Existing sales behavior stays the same: stock still deducts and receipts still render from the same order rows.
    - Legacy rows with no snapshot remain readable so reporting can fall back safely.
  </behavior>
  <action>
    Add a nullable `cost_at_sale` column to `order_items`, update the sqlc insert/returning shape, and teach `createOrderWithStores` to fetch the live variant before each `CreateOrderItem` write so the snapshot is captured once, at sale time.

    Keep the existing order-total and stock-deduction flow unchanged; only add the cost snapshot needed for gross profit reporting.
  </action>
  <verify>
    <automated>go test ./internal/sales ./...</automated>
  </verify>
  <done>Order item rows persist the sale-time cost basis and sales tests prove the snapshot does not drift when variant cost changes later.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add monthly sales and gross profit reporting APIs</name>
  <files>db/migrations/009_add_reporting_read_models.up.sql, db/migrations/009_add_reporting_read_models.down.sql, db/queries/reporting.sql, db/sqlc/reporting.sql.go, internal/reporting/service.go, internal/reporting/handler.go, internal/reporting/service_test.go, cmd/server/main.go</files>
  <behavior>
    - Monthly sales returns revenue, order count, and average order value grouped by month.
    - Gross profit uses stored sale-time cost first and falls back to live variant cost only for legacy rows.
    - Routes are mounted under `/api/reports` so the ERP frontend can query them without special-casing sales endpoints.
  </behavior>
  <action>
    Create a dedicated reporting package with sqlc-backed read models for `GET /api/reports/monthly-sales?year=YYYY` and `GET /api/reports/gross-profit?year=YYYY&month=MM`.

    Use the order-item cost snapshot for COGS, keep the payloads satang-based, and leave formatting to the frontend.
  </action>
  <verify>
    <automated>go test ./internal/reporting ./...</automated>
  </verify>
  <done>The backend can answer monthly sales and gross profit queries with explicit cost-basis logic and the routes are mounted in the server.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist category ordering for ERP category management</name>
  <files>db/migrations/010_add_category_sort_order.up.sql, db/migrations/010_add_category_sort_order.down.sql, db/queries/catalog.sql, db/sqlc/catalog.sql.go, internal/catalog/service.go, internal/catalog/handler.go</files>
  <behavior>
    - Category list order is stable and comes from `sort_order`, not incidental database insertion order.
    - Reorder updates write the exact user-specified sequence so the ERP can mirror drag/drop or move-up/move-down controls.
    - Category create/edit behavior stays compatible with the phase-1 catalog API.
  </behavior>
  <action>
    Add `sort_order` to categories, make list queries order by `sort_order` then `name`, and expose a reorder endpoint that accepts an ordered list of category IDs and persists dense ordering values.

    Preserve existing create/edit/category tree behavior; this task only adds the ordering layer the ERP UI needs.
  </action>
  <verify>
    <automated>go test ./internal/catalog ./...</automated>
  </verify>
  <done>Categories can be reordered deterministically and the backend exposes a stable order for the ERP category panel.</done>
</task>

</tasks>

<verification>
Backend verification: order items now store sale-time cost, reporting routes return monthly sales and gross profit, and categories can be reordered deterministically.
</verification>

<success_criteria>
- Gross profit calculations use a captured cost basis instead of the live variant cost.
- Reporting endpoints are available under `/api/reports` and compile cleanly.
- Category ordering survives reloads and is exposed through the catalog API.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-01-SUMMARY.md`
</output>
