---
phase: 04-erp-management-reporting
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/erp/__tests__/vitest-setup.test.ts
autonomous: true
requirements: [PLAT-05]
must_haves:
  truths:
    - "ERP money values remain formatted in THB because the Vitest harness can exercise the shared formatter"
    - "The frontend can run ERP-specific Vitest tests"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "jsdom test runner"
    - path: "frontend/src/test/setup.ts"
      provides: "shared RTL setup"
    - path: "frontend/src/erp/__tests__/vitest-setup.test.ts"
      provides: "ERP test smoke check"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/test/setup.ts"
      via: "vitest setupFiles"
      pattern: "jsdom|setup.ts"
---

<objective>
Bootstrap the frontend test harness for Phase 4.

Purpose: make ERP UI work testable before the shell and workflows land.
Output: Vitest config, shared setup, and smoke coverage.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-UI-SPEC.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and wire the Vitest environment</name>
  <files>frontend/package.json, frontend/vitest.config.ts, frontend/src/test/setup.ts, frontend/src/erp/__tests__/vitest-setup.test.ts</files>
  <read_first>
    Read `frontend/package.json`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/phases/04-erp-management-reporting/04-RESEARCH.md` before editing.
  </read_first>
  <action>
    Add the frontend test script and the ERP UI test dependencies, configure Vitest for jsdom, and add a smoke test that proves the setup runs.
  </action>
  <acceptance_criteria>
    - `grep -n "vitest\|jsdom\|@testing-library/react\|@testing-library/jest-dom" frontend/package.json frontend/vitest.config.ts frontend/src/test/setup.ts` finds the test harness wiring.
    - `cd frontend && npm exec vitest run src/erp/__tests__/vitest-setup.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/vitest-setup.test.ts</automated>
  </verify>
  <done>The ERP test harness is ready for the shell and workflow plans.</done>
</task>

</tasks>

<verification>
Frontend verification: Vitest runs in jsdom and can execute the ERP smoke test.
</verification>

<success_criteria>
- Vitest runs in the frontend workspace.
- ERP UI tests can import shared setup.
- The smoke test passes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-04-SUMMARY.md`
</output>
