---
phase: 04-erp-management-reporting
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - frontend/src/routeTree.gen.ts
  - frontend/src/routes/erp.reports.tsx
  - frontend/src/erp/reports/ReportDashboard.tsx
  - frontend/src/erp/reports/ReportCards.tsx
  - frontend/src/erp/reports/ReportChart.tsx
  - frontend/src/lib/reporting-api.ts
  - frontend/src/erp/reports/ReportExportButtons.tsx
  - frontend/src/erp/reports/exportReport.ts
  - frontend/src/erp/__tests__/reporting.test.tsx
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, PLAT-05]
must_haves:
  truths:
    - "Owner can switch to a reporting tab and see monthly sales plus gross profit"
    - "Reporting numbers display in THB and are sourced from the backend read models"
    - "Owner can export the active report view to PDF or XLSX without changing the underlying query state"
  artifacts:
    - path: "frontend/src/routes/erp.reports.tsx"
      provides: "Reporting tab route"
    - path: "frontend/src/erp/reports/ReportDashboard.tsx"
      provides: "Monthly sales and gross profit dashboard"
    - path: "frontend/src/erp/reports/exportReport.ts"
      provides: "PDF/XLSX export helpers"
    - path: "frontend/src/erp/__tests__/reporting.test.tsx"
      provides: "Reporting UI and export coverage"
  key_links:
    - from: "frontend/src/erp/reports/ReportDashboard.tsx"
      to: "frontend/src/lib/reporting-api.ts"
      via: "TanStack Query data fetch"
      pattern: "useQuery|monthly-sales|gross-profit"
    - from: "frontend/src/erp/reports/exportReport.ts"
      to: "frontend/src/erp/reports/ReportDashboard.tsx"
      via: "export the active query result"
      pattern: "jspdf|autotable|xlsx"
    - from: "frontend/src/erp/reports/ReportCards.tsx"
      to: "frontend/src/lib/formatCurrency.ts"
      via: "satang-to-THB formatting"
      pattern: "formatTHB"
---

<objective>
Build the ERP reporting tab with gross-profit analytics and export actions.

Purpose: let owners see sales performance and export the same filtered data to PDF or Excel from the desktop ERP surface.
Output: reporting route, dashboard cards/charts, export helpers, and Vitest coverage.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
@.planning/phases/04-erp-management-reporting/04-UI-SPEC.md

<interfaces>
From `frontend/src/lib/formatCurrency.ts`:
```ts
export function formatTHB(amount: number): string
```

From `frontend/src/lib/api.ts`:
```ts
export interface Product { id: string; name: string; category_id: string | null; image_url: string | null; is_active: boolean }
export interface Variant { id: string; product_id: string; sku: string; barcode: string | null; name: string; price: number; cost: number | null; is_active: boolean }
```

From `04-RESEARCH.md`:
```text
Gross profit must use a sale-time cost basis because live variant cost can drift.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the reporting route and KPI/chart dashboard</name>
  <files>frontend/src/routeTree.gen.ts, frontend/src/routes/erp.reports.tsx, frontend/src/erp/reports/ReportDashboard.tsx, frontend/src/erp/reports/ReportCards.tsx, frontend/src/erp/reports/ReportChart.tsx, frontend/src/lib/reporting-api.ts</files>
  <behavior>
    - The reporting tab fetches monthly sales and gross profit data with TanStack Query.
    - KPI cards show revenue, order count, average order value, and gross profit in THB.
    - The chart panel renders actual report data, not placeholder numbers, and respects the locked desktop ERP styling.
  </behavior>
  <action>
    Build the reporting route and dashboard composition on top of the shell from Plan 02, using Recharts for the visual panel and the shared THB formatter for every displayed monetary value.

    Keep the query state local to the reporting tab so management filters and reporting filters do not bleed into each other.
  </action>
  <acceptance_criteria>
    - `frontend/src/routes/erp.reports.tsx` exists and renders the reporting dashboard.
    - `frontend/src/erp/reports/ReportDashboard.tsx` imports the reporting API hooks and renders KPI cards.
    - `frontend/src/erp/reports/ReportChart.tsx` imports `recharts` and receives real query data.
    - `frontend/src/erp/reports/ReportCards.tsx` formats money via `formatTHB`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/reporting.test.tsx</automated>
  </verify>
  <done>The reporting tab shows monthly sales and gross profit from backend data with a charted dashboard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add PDF and XLSX export actions for the active report</name>
  <files>frontend/src/erp/reports/ReportExportButtons.tsx, frontend/src/erp/reports/exportReport.ts, frontend/src/erp/__tests__/reporting.test.tsx</files>
  <behavior>
    - Export uses the exact active report dataset and filters, not a separate fetch path.
    - PDF export uses `jspdf` plus `jspdf-autotable`; Excel export uses `xlsx`.
    - Filenames encode the report period so owners can match downloaded files to the screen they exported.
  </behavior>
  <action>
    Implement export buttons that transform the current monthly-sales or gross-profit query result into PDF and Excel downloads, keeping the tab state untouched after the download starts.

    Reuse the same report rows for both formats so the export matches the visible dashboard.
  </action>
  <acceptance_criteria>
    - `frontend/src/erp/reports/ReportExportButtons.tsx` references both `jspdf` and `xlsx`.
    - `frontend/src/erp/reports/exportReport.ts` formats tabular rows with the active filters.
    - `frontend/src/erp/__tests__/reporting.test.tsx` covers export-button behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend &amp;&amp; npm exec vitest run src/erp/__tests__/reporting.test.tsx</automated>
  </verify>
  <done>Owners can export the exact reporting view they are looking at to PDF or XLSX.</done>
</task>

</tasks>

<verification>
Reporting verification: the reporting route loads monthly sales and gross profit, and the export actions emit PDF/XLSX from the same filtered dataset.
</verification>

<success_criteria>
- The reporting tab shows monthly sales summary and gross profit from backend read models.
- Export buttons create PDF and XLSX files from the active report data.
- Reporting and export tests pass in Vitest.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-03-SUMMARY.md`
</output>
