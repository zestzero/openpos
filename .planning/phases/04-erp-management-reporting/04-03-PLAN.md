---
phase: 04-erp-management-reporting
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/010_add_category_sort_order.up.sql
  - db/migrations/010_add_category_sort_order.down.sql
  - db/queries/catalog.sql
  - db/sqlc/catalog.sql.go
  - internal/catalog/service.go
  - internal/catalog/handler.go
autonomous: true
requirements: [PROD-04]
must_haves:
  truths:
    - "Categories have a persisted sort order"
    - "Owners can reorder categories predictably from the ERP surface"
    - "Category listing stays compatible with existing catalog routes"
  artifacts:
    - path: "db/migrations/010_add_category_sort_order.up.sql"
      provides: "Category sort_order column"
    - path: "db/queries/catalog.sql"
      provides: "Category ordering and reorder query"
    - path: "internal/catalog/service.go"
      provides: "Category reorder logic"
  key_links:
    - from: "internal/catalog/service.go"
      to: "db/queries/catalog.sql"
      via: "list and reorder category sort_order"
      pattern: "sort_order"
    - from: "internal/catalog/handler.go"
      to: "internal/catalog/service.go"
      via: "reorder endpoint validation"
      pattern: "reorder"
---

<objective>
Persist category order for the ERP management surface.

Purpose: keep category ordering stable for desktop ERP tables and drawers.
Output: category sort_order migration, reorder query, and handler coverage.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-erp-management-reporting/04-CONTEXT.md
@.planning/phases/04-erp-management-reporting/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add category sort_order persistence</name>
  <files>db/migrations/010_add_category_sort_order.up.sql, db/migrations/010_add_category_sort_order.down.sql, db/queries/catalog.sql, db/sqlc/catalog.sql.go, internal/catalog/service.go</files>
  <read_first>
    Read `db/queries/catalog.sql`, `internal/catalog/service.go`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, and `.planning/phases/04-erp-management-reporting/04-CONTEXT.md` before editing.
  </read_first>
  <action>
    Add `sort_order` to categories, order the list query by `sort_order` then `name`, and add service methods that persist a dense reordered sequence.
  </action>
  <acceptance_criteria>
    - `grep -n "sort_order" db/migrations/010_add_category_sort_order.up.sql db/queries/catalog.sql internal/catalog/service.go` finds the new ordering path.
    - `go test ./internal/catalog ./...` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/catalog ./...</automated>
  </verify>
  <done>Categories can be reordered deterministically from service code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose category reorder behavior through the handler</name>
  <files>internal/catalog/handler.go, internal/catalog/service.go</files>
  <read_first>
    Read `internal/catalog/handler.go`, `internal/catalog/service.go`, `.planning/ROADMAP.md`, and `.planning/phases/04-erp-management-reporting/04-UI-SPEC.md` before wiring the handler.
  </read_first>
  <action>
    Add a reorder endpoint that accepts an ordered list of category IDs, validates the payload, and delegates to the service without changing existing create/edit routes.
  </action>
  <acceptance_criteria>
    - `grep -n "reorder\|sort_order" internal/catalog/handler.go` finds the new endpoint.
    - `go test ./internal/catalog ./...` passes.
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/catalog ./...</automated>
  </verify>
  <done>ERP category ordering is available through the catalog HTTP API.</done>
</task>

</tasks>

<verification>
Backend verification: category ordering persists and reorder requests are exposed through the catalog API.
</verification>

<success_criteria>
- Categories have stable persisted ordering.
- The catalog API can reorder categories.
- Catalog tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-erp-management-reporting/04-03-SUMMARY.md`
</output>
