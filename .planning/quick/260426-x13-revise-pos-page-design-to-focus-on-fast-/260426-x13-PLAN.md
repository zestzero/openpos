---
phase: 260426-x13-revise-pos-page-design-to-focus-on-fast-selling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/routes/pos.tsx
  - frontend/src/routes/pos.scan.tsx
  - frontend/src/pos/layout/PosLayout.tsx
  - frontend/src/pos/components/SearchBar.tsx
  - frontend/src/pos/components/QuickKeysBar.tsx
  - frontend/src/pos/components/CartPanel.tsx
  - frontend/src/pos/components/CatalogCategoryNav.tsx
  - frontend/src/pos/components/CatalogGrid.tsx
autonomous: true
requirements:
  - POS-01
  - POS-02
  - POS-03
  - POS-04
  - POS-05
  - POS-06
  - POS-07
must_haves:
  truths:
    - "Opening /pos feels like a selling workspace first, not a navigation hub"
    - "Search, quick keys, catalog browse, scan, and cart are all visible without hunting"
    - "The cart stays easy to scan while the cashier is adding items"
    - "The page keeps its thumb-reach workflow on small screens"
  artifacts:
    - path: "frontend/src/routes/pos.tsx"
      provides: "POS route composition and default selling workflow order"
    - path: "frontend/src/pos/layout/PosLayout.tsx"
      provides: "Shared POS shell, summary area, and bottom navigation framing"
    - path: "frontend/src/pos/components/SearchBar.tsx"
      provides: "Fast item lookup and add-to-cart entry point"
    - path: "frontend/src/pos/components/QuickKeysBar.tsx"
      provides: "One-tap repeat item shortcuts"
    - path: "frontend/src/pos/components/CartPanel.tsx"
      provides: "Cart review, quantity edits, and sale completion surface"
    - path: "frontend/src/pos/components/CatalogCategoryNav.tsx"
      provides: "Category filtering for touch catalog browsing"
    - path: "frontend/src/pos/components/CatalogGrid.tsx"
      provides: "Touch-first product grid for fast add-to-cart"
    - path: "frontend/src/routes/pos.scan.tsx"
      provides: "Dedicated scan workflow entry still reachable from the selling shell"
  key_links:
    - from: "frontend/src/routes/pos.tsx"
      to: "frontend/src/pos/layout/PosLayout.tsx"
      via: "child composition and shell ordering"
      pattern: "<PosLayout>"
    - from: "frontend/src/pos/components/SearchBar.tsx"
      to: "frontend/src/pos/hooks/useCart.ts"
      via: "search result add-to-cart action"
      pattern: "addItem\("
    - from: "frontend/src/pos/components/QuickKeysBar.tsx"
      to: "frontend/src/pos/hooks/useCart.ts"
      via: "favorite-item one-tap add action"
      pattern: "recordAdd\("
    - from: "frontend/src/pos/components/CatalogCategoryNav.tsx"
      to: "frontend/src/pos/components/CatalogGrid.tsx"
      via: "category state and grid filtering"
      pattern: "categoryId"
---

<objective>
Redesign the POS page so a salesperson can move from finding an item to adding it to the cart and completing the sale with less visual friction.

Purpose: Make the POS screen feel optimized for fast selling, not general browsing.
Output: Updated POS shell, route composition, and tuned selling surfaces.
</objective>

<execution_context>
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/zestzero/Documents/work-dir/openpos/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@AGENTS.md
@frontend/src/routes/pos.tsx
@frontend/src/routes/pos.scan.tsx
@frontend/src/pos/layout/PosLayout.tsx
@frontend/src/pos/components/SearchBar.tsx
@frontend/src/pos/components/QuickKeysBar.tsx
@frontend/src/pos/components/CartPanel.tsx
@frontend/src/pos/components/CatalogCategoryNav.tsx
@frontend/src/pos/components/CatalogGrid.tsx

<interfaces>
From frontend/src/pos/layout/PosLayout.tsx:
```typescript
interface PosLayoutProps {
  children?: ReactNode
}
```

From frontend/src/pos/components/CatalogCategoryNav.tsx:
```typescript
interface CatalogCategoryNavProps {
  selectedCategory: string | null
  onSelectCategory: (categoryId: string | null) => void
}
```

From frontend/src/pos/components/CatalogGrid.tsx:
```typescript
interface CatalogGridProps {
  categoryId: string | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose the POS shell around the selling flow</name>
  <files>frontend/src/routes/pos.tsx, frontend/src/pos/layout/PosLayout.tsx, frontend/src/routes/pos.scan.tsx</files>
  <action>Reorder the visible POS workspace so the page reads as find-item → add-item → review-cart → finish-sale. Keep the route guard and route separation intact, but reduce competing chrome in the shell, make the cart summary feel primary, and keep scan as an obvious secondary entry point rather than a separate mental model.</action>
  <verify><automated>pnpm --dir frontend build</automated></verify>
  <done>The default POS screen presents a clear selling-first hierarchy and still compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Tune the search, quick-key, catalog, and cart surfaces</name>
  <files>frontend/src/pos/components/SearchBar.tsx, frontend/src/pos/components/QuickKeysBar.tsx, frontend/src/pos/components/CartPanel.tsx, frontend/src/pos/components/CatalogCategoryNav.tsx, frontend/src/pos/components/CatalogGrid.tsx</files>
  <action>Refine spacing, touch targets, labels, and empty states so the cashier can search, tap quick keys, browse categories, and adjust cart quantity without scanning the UI for affordances. Preserve the existing TanStack Query and cart-hook wiring; change presentation and interaction hierarchy only.</action>
  <verify><automated>pnpm --dir frontend build</automated></verify>
  <done>The POS selling controls feel like one cohesive workflow with faster visual scanning and easier taps.</done>
</task>

</tasks>

<verification>
1. `pnpm --dir frontend build` succeeds.
2. In a mobile-width viewport, `/pos` foregrounds search, quick keys, and cart review without forcing extra navigation.
3. `/pos/scan` and `/pos/catalog` remain clearly reachable from the selling shell.
</verification>

<success_criteria>
- Cashiers can recognize the primary selling actions immediately on first load.
- The cart remains readable while products are being found and added.
- The POS page favors speed and one-handed use over dense general-purpose navigation.
- The redesign ships without breaking the existing route/component contracts.
</success_criteria>

<output>
After completion, create `.planning/quick/260426-x13-revise-pos-page-design-to-focus-on-fast-/260426-x13-SUMMARY.md`
</output>
